# Execution Log

Assumptions and judgement calls made **while executing** a step — the small decisions taken without
asking, the places where reality differed from the plan, and the things believed but not yet
verified.

## What belongs here, and what does not

| Register | Holds |
|---|---|
| [PLAN.md](PLAN.md) decisions log (`D-xx`) | Decisions **chosen by the user** in answer to a question, before execution |
| [PLAN.md](PLAN.md) findings (`R-xx`) | Risks that affect more than one phase |
| [docs/adr/](docs/adr/) | Architectural decisions with alternatives and a revisit condition |
| **This file** (`A-xxx`) | Everything settled **during** a step — assumptions, small choices, deviations, discoveries, corrections |

The distinction that matters: a `D-xx` was offered as a question and answered. An `A-xxx` was not
worth interrupting for, but is still the reason some piece of code looks the way it does.

## Conventions

- **IDs are permanent.** A superseded entry is marked, never renumbered or deleted.
- **Kind** is one of:
  - `assumption` — believed true, not verified. These carry risk.
  - `choice` — a judgement call where a reasonable alternative existed.
  - `deviation` — differs from the plan, the specification, or a stated intent.
  - `discovery` — learned during execution and changed something.
  - `correction` — something was wrong and was fixed.

---

## Open assumptions

Unverified beliefs currently load-bearing. Each needs confirming at the phase named.

| ID | Assumption | Confirm at |
|---|---|---|
| A-032 | CUDA minor-version compatibility lets a cu126 build run on driver 555.97 (reports CUDA 12.5) | Held so far — GPU matmul and both models ran. Watch for driver-related failures under sustained load. |
| A-034 | PP-OCRv6 on CPU takes roughly 8–15 s per scanned page | Phase 5, first real OCR run |
| A-035 | PaddleOCR-VL on CPU takes roughly 60–120 s per complex page | Phase 5 |
| A-036 | Gemma 3 4B at Q4 plus KV cache occupies ~3.5 GB, leaving room for the retrieval models | Phase 8, when a model is first loaded |
| A-037 | A ~2,500-token prompt prefills in ~700 ms on this GPU | Phase 11, first real generation |
| A-038 | A Supabase round trip costs 40–120 ms from this machine | Phase 2, first real connection |
| A-041 | A 400-page textbook yields ~1,000 child and ~300 parent chunks, ~25–35 MB of database | Phase 7 |
| A-042 | Page renders at 200 DPI average ~250 KB each | Phase 5 |
| A-098 | Extraction confidence below 0.6 is worth surfacing to the student as doubtful | Phase 5, against real OCR output. A placeholder with no data behind it. |

Everything in this table feeds a latency or capacity target in
[REQUIREMENTS.md](REQUIREMENTS.md). If one proves wrong, the target derived from it moves with it.

---

# Phase 0 — Foundation

## Step 0.1 — Repository skeleton

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-001 | deviation | Four directories added beyond the specified tree: `backend/scripts/`, `backend/tests/fixtures/`, `backend/alembic/versions/`, `docs/adr/` | The specification's tree omits them but the plan requires each. Alembic will not run without `versions/`. |
| A-002 | choice | Governing documents at repository root; ADRs under `docs/adr/` | Root placement makes them discoverable without hunting; ADRs are numerous enough to warrant a directory. |
| A-003 | choice | `__init__.py` in every `app/` directory, `.gitkeep` elsewhere | A Python package *is* its `__init__.py`. Using `.gitkeep` there would be replaced in the next step anyway. |
| A-004 | choice | `.gitattributes` added, not in the plan | A cross-platform Python and Node repository on Windows will otherwise accumulate mixed line endings, and the resulting diffs are unreadable. |
| A-005 | choice | `data/`, `storage/`, `uploads/` gitignored | The evaluation PDFs are private study material. An ignore rule is cheaper than discovering a textbook in the history. |
| A-006 | deviation | `main.py` and `pyproject.toml` deferred from 0.1 to 0.2 | Both are in the specified tree, but neither is meaningful before the Python project exists. |

## Step 0.13 — PLAN.md

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-007 | choice | The plan carries a decisions log, a risk register and a coverage matrix, beyond the phase checklists | A checklist records *what*; without the other three, the *why* survives only in conversation. |
| A-008 | choice | Risks numbered `R-xx` and given a "revisit at" phase | A risk with no named revisit point is a note, not a risk. |

## Step 0.8 — Functional requirements

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-009 | deviation | Domain-prefixed IDs (`FR-RET-04`) rather than the flat `FR-001` the plan specified | Flat numbering across 334 requirements makes insertion require renumbering, which breaks every test that cited an old number. |
| A-010 | choice | Requirements written in MUST / SHOULD / MUST NOT form | Descriptive prose cannot be tested. The modal verb is what makes a requirement checkable. |
| A-011 | choice | Prohibitions captured as explicit requirements | The specification states them as prose. A prohibition nobody wrote down is one that fails silently — nothing alerts you when a forbidden thing simply happens. |
| A-012 | choice | Derived requirements trace to a decision ID instead of a specification section | Keeps visible which requirements came from the source document and which from our own decisions. |
| A-013 | choice | 32 domains rather than the plan's nine | Nine groups of forty requirements are not navigable. The finer split makes a domain's whole surface reviewable at once. |

## Step 0.9 — Non-functional requirements

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-014 | choice | Latency budgets derived from a published stage-by-stage cost model rather than asserted | A number with no derivation cannot be argued with, and cannot be corrected when one of its inputs turns out wrong. |
| A-015 | discovery | The cost model showed network round trips to Supabase are the largest non-model cost — roughly 360 ms of the 1.8 s budget | A consequence of hosting that was not obvious when the hosting was chosen. Recorded rather than absorbed. |
| A-016 | choice | Release gates are checked from the phase that introduces each surface, not deferred to Phase 17 | A gate first exercised at the end is a gate that finds problems when they are most expensive. |
| A-017 | choice | Several NFRs written as structural rather than aspirational — an unscoped query must fail *at construction*, RLS presence is asserted by enumerating tables | "Remember to do this" and "cannot be done wrong" are different requirements. Only the second survives a tired afternoon. |
| A-018 | choice | `NFR-PERF-17` added: VL fallback exceeding 20% of pages means the classifier is miscalibrated | The specification says the heavy model must not run everywhere but gives no threshold, so the rule was untestable. |

## Step 0.10 — ARCHITECTURE.md

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-019 | choice | The layers table states what each layer must **not** contain | The prohibition is the part a test can be written against. |
| A-020 | deviation | Six diagrams rather than the two the plan named | Layer dependency, scope enforcement and the model gateway each turn on a relationship that prose describes poorly. |
| A-021 | choice | The scaling path gives a **trigger** for each step, not just an ordering | "Add workers" without "when queue depth is persistently non-zero" is not actionable, and invites adding them on instinct. |
| A-022 | choice | A closing section on what the architecture deliberately is not | Negative constraints erode quietly. A future phase is likelier to reach for a framework if nobody recorded why it is absent. |

## Step 0.11 — Architecture decision records

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-023 | choice | Every ADR ends with a **revisit condition** | A decision with no falsifying condition is a belief that hardens into folklore. |
| A-024 | discovery | ADR-0011 gained **structured-output validity rate** as a benchmark measure, beyond the five the specification lists | Quantization degrades instruction-following and schema fidelity before it degrades fluency. A model can read fine while failing exactly what this system validates. |
| A-025 | discovery | With the graph in PostgreSQL, provenance can be enforced by a `NOT NULL` constraint rather than application discipline | Turns the zero-tolerance gate on provenance-free edges from a code property into a schema property. Strengthens ADR-0012 beyond what motivated it. |
| A-026 | choice | ADR-0014 documents why crops stay permanent while page renders do not | The asymmetry is not obvious: recovering a 60 KB crop needs the original, a re-render and a bounding box, and it is read on every visual question. |

## Step 0.12 — Use cases

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-027 | discovery | `FR-OBJ-08` — exploring relationships through a concept graph — was a stated product objective with **no use case**. Added UC-23 and UC-24 | Found by checking the use cases against the nine primary objectives rather than assuming coverage. |
| A-028 | deviation | All 24 use cases written in full; the plan said early ones in full and later ones stubbed | Stubs would mean later phases define their own acceptance criteria, which inverts the point of writing them in advance. |
| A-029 | choice | Numbering appended rather than inserted when adding UC-23 and UC-24 | Earlier phases already reference UC-01 … UC-22. |
| A-030 | discovery | Writing exception flows forced five behavioural decisions that would otherwise have been made ad hoc mid-implementation | A selected region matching no object falls back to page level **and says which page**; an over-committed study plan **states the shortfall** rather than compressing; a truncated graph neighbourhood **indicates truncation**; one page failing OCR does not fail the document; partial evidence answers what it can and **names** what it cannot. |

## Step 0.2 — Backend project

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-031 | deviation | The `ml` dependency group deferred to 0.3 | Forced, not preferred: `paddlepaddle-gpu` is not resolvable from the public index without a CUDA-version-specific source, and the CUDA version was unknown. Declaring it would have made the lock fail outright. |
| A-039 | choice | All runtime dependencies in dependency groups, none in project dependencies | This is what makes the isolation real — a broken ML install cannot block work on the rest of the backend. |
| A-040 | choice | `pgvector` and `tenacity` added to core, beyond the plan's list | The first supplies the column type the schema needs; the second the bounded-retry primitive the model gateway requires. Both would have been added later anyway, at the cost of a lockfile churn. |
| A-043 | choice | PyJWT over python-jose | Better maintained. python-jose has a history of advisories and is close to unmaintained. |
| A-044 | choice | `httpx2` added to dev | Starlette 1.3 deprecated `httpx` for its test client; without it every test run from Phase 3 onward emits a deprecation warning. |
| A-045 | choice | `main.py` deliberately thin, with liveness and readiness explicitly separated in a comment | Conflating them is a common mistake, and one that makes a health check useless precisely when a dependency is down. |

## Step 0.3 — GPU and ML dependencies

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-033 | choice | torch resolved from the **cu126** index, not cu128 or cu129 | Driver 555.97 reports CUDA 12.5. Minor-version compatibility covers 12.x on a driver ≥ 525; a newer runtime would want a newer driver. |
| A-046 | discovery | 6 GB VRAM is the binding constraint on the whole design — recorded as R-07 | Measured, not assumed. It forced OCR onto CPU, made `Q8_0` quantization probably unselectable, and is the reason keeping all models warm is possible at all. |
| A-047 | discovery | Cross-encoder scores are low and compressed: −10.58 relevant against −11.24 irrelevant — recorded as R-08 | Empirical confirmation that an absolute score threshold would discard every candidate. Directly shaped the configuration schema two steps later. |
| A-048 | deviation | Ingestion latency targets revised upward after moving OCR to CPU; two full-document budgets added | The original targets assumed GPU OCR. Chat targets were left unchanged, since the GPU is no longer contended. |

## Step 0.4 — Code-quality tooling

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-049 | choice | mypy strict on the domain and application layers only | Those depend on nothing external, so there is no third-party typing to fight. Strictness at the adapter edge adds noise without safety. |
| A-050 | choice | Ruff enables ASYNC, S and DTZ beyond a conventional set | Async correctness is load-bearing given the async data layer; security is a first-class concern; naive datetimes in memory validity or job leases are silently wrong, which is the worst kind. |
| A-051 | choice | Three pytest markers registered before anything needs them | Phase 17 must consolidate the security suite and release gates into something selectable. Tagging from the start is far cheaper than retrofitting. |
| A-052 | correction | Source files read as `utf-8-sig`, not `utf-8` | Found by planting a probe file written with a BOM: `ast.parse` died with a confusing `SyntaxError` before reaching the import check. On Windows a BOM is easy to introduce. |
| A-053 | choice | The first tests are the dependency-rule guard and an application boot check, not placeholders | A trivial passing test proves the runner works and nothing else. |

## Step 0.5 — Frontend scaffold

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-054 | choice | Scaffolded by hand rather than with the Vite generator | The generator would have overwritten the directory tree established in 0.1. |
| A-055 | choice | TypeScript stricter than the default template — unchecked index access, exact optional properties, verbatim module syntax | The frontend mirrors the backend contract in Zod; loose typing here defeats having the contract. |
| A-056 | choice | Semantic tokens for abstention and conflict states defined in the stylesheet immediately | Abstentions and source conflicts must be visually distinct from ordinary answers. Establishing the vocabulary now is far cheaper than retrofitting it once the chat interface exists. |
| A-057 | choice | The query client does not retry 401 or 404 | A Knowledge Base the user does not own returns 404 by design, so a retry can never succeed and only adds latency. |
| A-058 | choice | `.claude/launch.json` added, not in the plan | Both processes get a named launch configuration, so later phases do not rediscover the commands. |
| A-059 | correction | The CSS Modules test asserts the class is *scoped*, not that it matches a particular scoping pattern | The first version checked Vite's `[name]__[local]___[hash]` format, which differs between development, test and production builds, so it failed under the test runner. |
| A-060 | correction | Type-aware linting scoped to TypeScript files, with type checking disabled for JavaScript | The first configuration applied typed rules to the ESLint config file itself, which the TypeScript project does not cover — producing a stack trace rather than a lint error. |
| A-061 | choice | The React hooks plugin registered explicitly rather than through its preset | Its shipped configuration is not flat-config shaped in the current major version; spreading the rules object is version-proof. |

## Step 0.6 — Configuration schema

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-062 | choice | Sixteen settings groups, each with its own environment prefix, rather than one nested structure | Prefixed variables read better in an env file than nested delimiters, and each group can be constructed independently in a test. |
| A-063 | choice | **No absolute reranker threshold setting exists.** Only a relative margin | Following directly from A-047. A plausible-looking `EVIDENCE_SCORE_THRESHOLD=0.0` would discard every candidate. Omitting the knob is more reliable than documenting that it is dangerous, and a test fails if one is reintroduced. |
| A-064 | choice | Seven invariants enforced at startup rather than documented | A misconfiguration that boots successfully is the one that reaches production. |
| A-065 | choice | `.env.example` and the schema kept in sync in **both** directions by tests | A declared key no setting reads is worse than an absent one — it looks configured and does nothing. |
| A-066 | choice | Ruff's ambiguous-unicode rules for docstrings and comments disabled; the identifier rule kept | Prose legitimately contains typographic dashes. Ambiguous characters in *identifiers* are the actual homoglyph hazard. |
| A-067 | correction | The secret-leakage test rewritten to inject a sentinel value | The first version's regular expression matched the literal wrapper text `service_role_key=SecretStr(`, so it was failing on the redaction working correctly. |
| A-068 | correction | The "no absolute threshold" test rewritten to parse assignment keys | The first version matched the word *absolute* in the explanatory comment. |

## Step 0.7 — Environment verification

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-069 | choice | SKIP and FAIL are distinct, and only FAIL sets a non-zero exit code | A script that reports failure on a machine nobody has configured yet is a script nobody runs until it is too late to help. |
| A-070 | choice | A third state, WARN, for configurations that are wasteful rather than broken | A GPU Paddle build when the configuration says CPU is worth saying and is not a failure. |
| A-071 | choice | Verification is live — a real GPU matmul, a real presigned URL fetch with byte comparison | Checking that a library imports proves considerably less than checking it works. |
| A-072 | discovery | Ollama is not installed on this machine | Confirmed rather than inferred. Moved in the inputs table to the phase that actually needs it. |

## Cross-cutting revisions

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-073 | deviation | The "no identifiers in comments" instruction extended to **validation error message strings**, beyond the literal request | An operator hitting an error at two in the morning should not need the requirements register open. Flagged at the time so it could be reverted. |
| A-074 | correction | `PLAN.md` was corrupted by a PowerShell read-write round trip that re-encoded every typographic character; restored from the index and the edits redone | PowerShell 5.1 reads as ANSI and writes as UTF-8. Editing files through the shell was abandoned. |

---

# Phase 1 — Domain layer

## Step 1.1 — Domain vocabulary

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-075 | choice | Behaviour attached to enum members rather than placed in services | Routing rules are stated once and testable directly. Scattered conditionals drift apart. |
| A-076 | choice | `MemoryProvenance` added as an ordered enum, beyond the plan's list | "A recent correction outranks an assistant guess" is an ordering. Modelling it as one avoids reimplementing the comparison in Phase 14. |
| A-077 | choice | Ten enums included beyond the plan's named list — page kind, processing method, job status, message role, graph node type, model task, claim status, retriever kind, memory type, memory provenance | Each is needed by an entity in steps 1.2 to 1.6. Splitting them across steps would mean editing this module five more times. |
| A-078 | deviation | Study-content enums deliberately excluded | Summary kinds and question types belong with the entities in Phase 15, and were not in the step's stated scope. |
| A-079 | choice | `BoundingBox` carries no page number | A box is a region; which page it is on belongs to whatever owns the box. Two sources of truth for the same fact is how they diverge. |
| A-080 | choice | `BoundingBox` given intersection-over-union and merge now rather than in Phase 6 | Associating captions with figures needs both. Geometry reimplemented at three call sites is geometry wrong at two of them. |
| A-081 | choice | `TokenBudget.allocate` raises rather than clamping | Silently truncating would build an answer on less evidence than the caller believes it supplied. A separate method exists for the shedding path, where not fitting is expected. |
| A-082 | choice | `ScopeContext.__str__` truncates identifiers to eight characters | It ends up in log lines, where full UUIDs are noise rather than information. |
| A-083 | choice | `NotFoundError` and `ScopeViolationError` kept as distinct types despite mapping to the same response | A resource the caller does not own must be indistinguishable from one that does not exist, or the API becomes an oracle for guessing identifiers. Distinct internally is what lets one be logged as a security event and the other not. |
| A-084 | choice | `CoverageStatus.CONFLICTING` does not trigger another retrieval round | More searching will not resolve sources that genuinely disagree. The disagreement is the finding. |
| A-085 | correction | Exception classes renamed with the `Error` suffix | Ruff's naming check enforcing the language convention. It was right. |
| A-086 | choice | `JobType.SYNC_GRAPH_PROJECTION` has a member name that differs from its stored value `SYNC_NEO4J` | The stored value keeps schema compatibility with the specified job-type set; the member name describes what it would actually do. |
| A-087 | correction | An enum test asserts string behaviour through `str()` and `.value` rather than direct comparison | A member whose name and value deliberately differ makes the direct comparison unprovable to the type checker, though it holds at runtime. |

## Step 1.2 — Knowledge Base and document entities

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-088 | choice | `UntrustedText` introduced as a type, and `DocumentElement.text` uses it | Document text must reach a prompt as evidence, never as instruction. A type carries that provenance where a convention would not. Its `__str__` returns a placeholder rather than the content, so accidental interpolation into a prompt template produces visibly wrong output instead of handing a document author the system prompt. |
| A-089 | choice | `app/domain/invariants.py` added for shared validation helpers | Every entity checks the same handful of things. Written once, the messages stay consistent and the checks cannot quietly diverge across six modules. |
| A-090 | choice | `KnowledgeBase.scope` derives the scope from the entity itself | Nothing downstream should have to pair a user identifier with a Knowledge Base identifier by hand and risk pairing them wrongly. Every scoped entity now reports its own scope. |
| A-091 | choice | `COMPLETED → PROCESSING` is permitted; `COMPLETED → PENDING` is not | Reprocessing after an embedding-model change must be possible. Returning to pending would describe an already-ingested document as never having been ingested. |
| A-092 | choice | `DELETING` is an absorbing state with no outbound transitions | A path back would make content retrievable again after its files had been removed. |
| A-093 | choice | Transitions take `now` as a required argument; the domain reads no clock | Behaviour stays reproducible and no test has to freeze a global. |
| A-094 | choice | A failed document must carry a reason, and a non-failed one must not | A failure nobody can act on tells the student only that something went wrong; a stale reason on a recovered document misreports its state. |
| A-095 | choice | A completed document must know its page count | Completion means the pipeline ran to the end, which is not describable without knowing what it covered. |
| A-096 | choice | An element produced by OCR must carry both a bounding box and a confidence | It was read from a rendered region, so its location is known. A citation that cannot be opened at a location is not much of a citation. |
| A-097 | choice | `ExplanationLevel` added to the enums, with three levels | The Knowledge Base carries an explanation level but the specification does not enumerate one. A free string would have no closed set to route on. |
| A-098 | assumption | Low extraction confidence is defined as below 0.6 | A placeholder threshold with no data behind it. Should be calibrated once real OCR output exists — recorded in the open assumptions table. |
| A-099 | discovery | mypy was not checking the test suite at all | Two `conftest.py` files without package markers collided on module name, and mypy stops after that error. Adding `__init__.py` to the test directories fixed it and immediately surfaced eleven genuine annotation gaps. |
| A-100 | correction | Fixture factories typed with a generic `Builder[T]` protocol rather than silenced with ignore comments | The first version scattered `# type: ignore[no-untyped-def]` at every call site, which switches type checking off for the test that uses it — the opposite of what running mypy over tests is for. |
| A-101 | correction | Test directories became packages, and their `.gitkeep` placeholders were removed | Required by the module-name fix. It also makes the relative imports between test modules legitimate rather than incidentally working. |
| A-102 | correction | A bulk regular-expression edit across the test files produced a syntax error and several over-long lines | Multi-line signatures were not matched by a pattern written for single-line ones. Repaired by hand. Bulk edits over Python signatures are not reliable; targeted edits are. |

## Step 1.3 — Retrieval entities

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-103 | choice | `Chunk` lives under documents rather than retrieval | It is derived from document elements and carries document identifiers, page numbers and a heading path. Retrieval consumes it; it is not owned by retrieval. |
| A-104 | choice | `EvidenceLabel` is a value object parsing and rendering `S1`, not a bare string | The model never sees a chunk identifier, so it cannot invent one that happens to exist. An invented label is either out of range or malformed, and both are caught by the type. |
| A-105 | choice | `EvidenceSet` refuses to span more than one scope, at construction | Mixed evidence is a leak that has already happened by the time anyone inspects it. Refusing it where it would be assembled is the last moment it is still preventable. |
| A-106 | choice | Citation resolution goes through `EvidenceSet.require`, never through a repository lookup | A chunk can be real, belong to the right student and the right Knowledge Base, and still not have been supplied for this question. Resolving against the database would wave that through; resolving against what was actually put in front of the model does not. |
| A-107 | choice | `Evidence` must record at least one retriever | Evidence that arrived from nowhere cannot be explained or reproduced, and retrieval decisions have to be inspectable after the fact. |
| A-108 | choice | Mandatory scope filters are deliberately absent from `RetrievalFilters` | It holds only optional narrowing. A field can be left unset, and the user, Knowledge Base and completed-status predicates must not be capable of being unset. |
| A-109 | choice | `RetrievalPlan.for_query` derives routing from the properties already on the query class | A second, separate set of rules would drift from the first. This reads as an application of the existing rules rather than a restatement. |
| A-110 | choice | An early exit is dropped when the object it depends on was not actually selected | Asking about "the table on page 67" without having selected one still needs a search to work out which table that is. |
| A-111 | choice | `RetrievalPlan` validates that graph retrieval and the graph flag agree, and that a shortcut never coexists with query expansion | Both are states that would silently produce the wrong pipeline rather than an error. |
| A-112 | choice | `Chunk.carries_a_visual` excludes tables | A table's structured form is authoritative, so answering from it does not require re-reading the picture. A figure's stored description is derived, so it does. |
| A-113 | choice | Compression may not lengthen text or empty a chunk, and does not recompute the token count | Compression that adds text is generation. The caller knows the tokenizer, and a guessed count would corrupt the context builder's budget. |
| A-114 | discovery | `enums.py` already contained an `EarlyExitPath`, added outside this session, and my edit created a duplicate class definition | Caught by the linter's redefinition check. Reconciled rather than overwritten: the pre-existing member names describe the path taken (`TABLE_LOOKUP`) rather than its trigger, which reads better at the call site, so those were kept. |
| A-115 | deviation | "No early exit" is `None` rather than an enum member | Follows from A-114 — the pre-existing enum had no member standing for absence, and adding one would have been wrong anyway. No shortcut is not a kind of shortcut. |

## Step 1.4 — Conversation and memory entities

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-116 | choice | `MessageStatus` gained `can_transition_to`, `successors`, and `is_terminal` on the enum itself | Consistent with `DocumentStatus`, which carries the same machinery. The state machine lives with the type it describes, not distributed across callers. |
| A-117 | choice | `Conversation` stores active_document_id, active_page_number, active_figure_id, and active_table_id as plain optional fields, not as a nested state object | The context is shallow — at most four fields — and the navigation methods are straightforward replaces. A nested state object would add indirection with no corresponding benefit. |
| A-118 | choice | Figure and table selections are mutually exclusive and enforced by `__post_init__` | At any moment the student has selected at most one visual object. Permitting both simultaneously would require every routing decision to resolve the tie — better to refuse the state upfront. |
| A-119 | choice | `focus_page`, `focus_figure`, and `focus_table` do not check `active_document_id` themselves; the invariant in `__post_init__` catches it on the resulting entity | A guard in the method and a guard in the invariant would be two statements of the same rule. A single guard in `__post_init__` is the one the frozen dataclass already guarantees will run. |
| A-120 | choice | `Message.content` is `UntrustedText` for both user and assistant roles | User input can carry injection attempts. Assistant output can carry injected content absorbed from documents and may reappear in a future turn's context. Uniform provenance tracking forces callers to use `.value` explicitly rather than interpolating content by accident. |
| A-121 | choice | `model_id`, `prompt_tokens`, and `completion_tokens` must be set together or not at all; `finish_reason` is independently optional | The three core fields come from a single provider response and are meaningless apart from each other. `finish_reason` varies by provider and is genuinely absent in some responses. |
| A-122 | choice | `MemoryFact.create_successor` receives `successor_id` from the caller rather than generating it internally | Callers can then construct both facts and write them atomically in a single transaction without a database round-trip to learn the generated key. The entity stays deterministic and testable with known identifiers. |
| A-123 | choice | Supersession is only permitted from `ACTIVE` status | A disputed, expired, or already-superseded fact was never the current truth; superseding it would imply it was. A correction on an UNCONFIRMED fact should delete it, not supersede it, since it was never confirmed as true. |
| A-124 | choice | `mark_deleted` blocks `SUPERSEDED` facts in addition to already-deleted ones | Deleting the retired half of a supersession chain would break the audit link. The old fact must stay in the database for as long as the new one lives. |
| A-125 | choice | `valid_until: None` means the fact is valid indefinitely | A mandatory expiry would require setting an arbitrary far-future sentinel for facts that do not expire (exam dates, permanent goals). `None` represents "no expiry" without encoding it as a magic value. |
| A-126 | choice | Both `Conversation` and `MemoryFact` expose a `scope` property that derives from the stored identifiers | Consistent with `KnowledgeBase`, `Document`, and `Chunk`. Any entity that belongs to a user and a Knowledge Base reports its own scope rather than requiring the caller to pair the identifiers by hand. |

## Step 1.5 — Graph entities

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-127 | choice | `GraphRelationship` provenance fields — `source_chunk_id`, `page_number`, `evidence` — are positional required fields with no defaults | The plan's requirement is "unrepresentable-if-absent at the type level". Required positional fields enforce this structurally: you cannot call the constructor without supplying them. A __post_init__ check alone could be bypassed by `dataclasses.replace`; required fields cannot. |
| A-128 | choice | `evidence` is `UntrustedText`, not a plain `str` | The evidence passage is a span extracted from document content. It carries the same injection risk as the source chunk's text and must not be interpolated into a prompt template without explicit `.value` access. |
| A-129 | choice | `GraphEntity` source provenance fields are optional | Structural nodes (KNOWLEDGE_BASE, DOCUMENT) exist in the schema independently of extraction and have no chunk provenance. Making the fields optional is the right trade-off; the per-type constraint belongs in the extraction service, not in the entity constructor. |
| A-130 | choice | Self-loops (`source_entity_id == target_entity_id`) are rejected at construction | A relationship that links an entity to itself is meaningless for any of the defined relationship types and most likely indicates an extraction error. |
| A-131 | choice | `weight` defaults to 1.0 but must be positive | An initial extraction gives equal weight to all edges. Algorithms that refine weights (community detection, PageRank-style scoring) may update it, but a zero or negative weight is not a valid graph weight in any standard formulation. |
| A-132 | choice | `extraction_confidence` is optional and independent of `weight` | Confidence is the model's estimate of correctness; weight is the traversal score. They start from different sources and are refined by different processes. Conflating them into one field would make it ambiguous which process last touched the value. |
| A-133 | choice | The boundary test now covers `app/domain/graph/` alongside the other domain subdirectories | Verified: the domain import audit still lists only `['__future__', 'app', 'dataclasses', 'enum', 'typing', 'uuid']`. `app.domain.graph.entities` imports nothing outside the domain. |

## Step 1.6 — Model and job entities

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-134 | assumption | The seven slots for `ModelRequest` are: system preamble, safety rules, task instructions, memory context, evidence, conversation history, query | The specification mentions a seven-slot structure and a prompt normalizer that maps it to a provider payload, but does not enumerate the slots in the sections available. The seven slots were derived from Phase 10's context builder description (instruction handling by priority level, followed by memory, evidence, history, and query). Confirm when Phase 10 is implemented. |
| A-135 | choice | `ConversationTurn` lives in `models/entities.py` alongside `ModelRequest`, not in `values.py` | It is a value type in terms of semantics but exists solely as a slot in `ModelRequest.conversation_history`. Placing it in `models/` makes the connection explicit. |
| A-136 | choice | `ModelRequest.query` is `str`, not `UntrustedText` | By the time a query reaches a `ModelRequest`, it has been through the query rewriter, which produces a normalised string. The context builder's responsibility is to use the rewritten form. Evidence and conversation history remain `UntrustedText` because they arrive from uncontrolled sources. |
| A-137 | choice | `ModelRequest.evidence` is `tuple[UntrustedText, ...]` | Retrieved document passages carry the same injection risk as the chunk text they came from. The prompt normalizer must handle them explicitly via `.value`, not by interpolation. |
| A-138 | choice | `ModelRequest.memory_context` is `tuple[str, ...]` | Memory facts are application-validated strings — their content is never directly from user input without a verification step. Treating them as trusted strings keeps the model prompt readable without hiding a real risk. |
| A-139 | choice | `ProcessingJob` placed in a new `app/domain/jobs/` subdirectory, not in `models/` | A job is a durability wrapper around async work, not a model artifact. Keeping them in separate packages makes the boundary visible in the import graph. |
| A-140 | choice | `attempt_count` increments on `claim`, not on `fail` | The count tracks how many attempts were *started*. A started attempt that crashes before reporting failure should still count. Incrementing on fail would undercount if the worker dies mid-run. |
| A-141 | choice | `fail` decides `FAILED` vs `DEAD_LETTER` by checking `attempt_count >= max_attempts` at the moment of failure, after claim has already incremented the count | This means the final attempt lands in `DEAD_LETTER` automatically — no separate transition is needed. `requeue` is then the path back to `PENDING` for intermediate failures, with a guard that rejects requeueing once the budget is spent. |
| A-142 | choice | `heartbeat` uses `dataclasses.replace` directly rather than `_transition` | Heartbeat does not change status — it extends `lease_expires_at` and updates `last_heartbeat_at`. Using `_transition` would require a `RUNNING → RUNNING` entry in the transition table, which would be misleading. |
| A-143 | choice | `scheduled_at` included on `ProcessingJob` for delayed jobs | Some jobs (summary rebuild, memory compaction) are enqueued with a future start time. `scheduled_at: None` means the job is immediately claimable; a set value means workers skip it until that time passes. |
| A-144 | choice | `payload: Mapping[str, str]` uses `collections.abc.Mapping` for a read-only view | The domain entity sees the payload as immutable. Callers pass a `dict[str, str]`, which satisfies `Mapping[str, str]`. The frozen dataclass prevents the field from being reassigned; the type annotation communicates that downstream code must not mutate it. |
| A-145 | correction | mypy reported "Incompatible types in assignment" because `ts` was reused across two `for` loops with different element types (`datetime` then `datetime | None`) | Python loop variables persist in scope after the loop ends, and mypy tracks the inferred type. Fixed by renaming to `req_ts` (required datetime) and `opt_ts` (optional datetime) in the respective loops. |
| A-146 | choice | `JobStatus.DEAD_LETTER → CANCELLED` is a permitted transition | An administrator may cancel a dead-lettered job rather than leaving it in the queue indefinitely. `DEAD_LETTER → PENDING` is not permitted — a dead-lettered job has exhausted its retry budget. |
| A-147 | choice | `requeue` blocks when `attempt_count >= max_attempts` rather than relying on the transition table alone | The enum cannot see `attempt_count`, so it marks `FAILED → PENDING` as valid universally. The entity guards the business rule: a failed job with no remaining attempts must not be requeued. The guard raises `InvariantViolationError` rather than `IllegalTransitionError` because the status transition itself is valid — it is the entity's internal state that forbids it. |
| A-148 | choice | `ModelResponse.content` is `UntrustedText` | Model output can reflect injected content absorbed from document passages or malicious conversation turns. Treating it as plain text would allow it to be interpolated into subsequent prompts or displayed without review. Downstream callers must access `.value` explicitly. |

## Step 1.7 — Repository ports

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-149 | choice | Ports placed in `app/domain/ports/`, not `app/application/ports/` | ARCHITECTURE.md section 4.1 explicitly lists ports in the Domain layer. The domain owns the contracts it depends on; infrastructure implements them by structural subtyping. The boundary test already walks all of `app/domain/` so no extra configuration was needed. |
| A-150 | choice | `KnowledgeBaseRepository.list_for_user` takes `user_id: UUID`, not `ScopeContext` | Listing KBs is how a session establishes which KB to work in — there is no KB id to put into a ScopeContext at that point. Documented as the first exception to the scope-first rule and tested explicitly: a test asserts the first non-self parameter is `user_id`, not `scope`. |
| A-151 | choice | `JobRepository` worker methods (`get`, `save`, `claim_next`) do not take `ScopeContext` | Workers pull from a shared queue across all users. Pre-scoping them would require manufacturing a scope for every job, coupling the worker to user management. `list_for_scope` still uses `ScopeContext` so the user-facing status path is correctly enforced. |
| A-152 | choice | All port methods are `async def` | D-25 requires an async data layer throughout. A sync implementation cannot satisfy an `async def` protocol method in mypy, so the constraint is enforced at type-check time rather than discovered at runtime. |
| A-153 | choice | `ConversationRepository` owns both `Conversation` and `Message` persistence | Messages are always accessed through their conversation. A separate `MessageRepository` would require callers to coordinate two repositories for every aggregate read, and would leave the conversation/message boundary visible to application-layer code that should not care about it. |
| A-154 | choice | `GraphRepository.delete_for_document` removes both entities and relationships in one call | A document deletion must clean up its extracted graph completely. Two separate delete methods would allow partial cleanup — entities removed but relationships left orphaned, or the reverse — and would require the caller to call both correctly every time. |
| A-155 | choice | Typed stub classes in `test_ports.py` rather than `@runtime_checkable` isinstance checks | `@runtime_checkable` only verifies that method names exist, not their signatures. Typed stubs are checked by mypy against the full protocol signature including parameter types and return types. The stubs also serve as working examples of what a conforming adapter looks like. |
| A-156 | choice | `inspect.signature` used in pytest to verify the scope-first rule at runtime | The scope-first constraint is a security property. mypy verification is necessary but not sufficient as a record — embedding it as a pytest assertion makes the invariant visible in the test suite, gives it a name, and produces a clear failure message if a future edit drops the `scope` parameter from a method. |

## Step 1.8 — Adapter ports

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-157 | choice | All 10 adapter ports placed in `app/domain/ports/adapters.py` alongside repositories | ARCHITECTURE.md places ports in the domain layer. A single file per concern (`repositories.py`, `adapters.py`) keeps the port surface browsable without scattering it across many files. |
| A-158 | choice | `PdfParserPort.parse` takes `scope: ScopeContext` as a keyword-only argument | The parser needs `user_id` and `knowledge_base_id` to produce correctly-scoped `DocumentPage` and `DocumentElement` objects. Making scope keyword-only reflects that it is a context parameter rather than part of the PDF data being processed. |
| A-159 | choice | `OcrPort.extract_text` takes `page: DocumentPage` rather than `scope: ScopeContext` directly | The page object already carries `user_id`, `knowledge_base_id`, `document_id`, and `page_number` — all the fields the OCR adapter needs to produce correctly-scoped elements. Passing scope separately would be redundant and would require the adapter to also receive document_id and page_number via additional parameters. |
| A-160 | choice | `EmbeddingPort` exposes separate `embed_documents` and `embed_query` methods | Some embedding models (BGE, E5) apply different instruction prefixes or pooling strategies for documents vs. queries. A single method would force the implementation to guess or accept a flag, both of which are error-prone. |
| A-161 | choice | `GraphPort` vocabulary is `neighbors` and `subgraph`, with no query language method | D-10 defers a graph database but requires the option to be available later. A port defined in graph-traversal vocabulary (`neighbors`, `subgraph`) means introducing a Neo4j adapter is a new file, not a change to callers. A method accepting a Cypher string would pin callers to Neo4j before it is even installed. |
| A-162 | choice | `StoragePort` and `CacheStore` do not take `ScopeContext` | Blob keys embed scope by convention (`{user_id}/{kb_id}/...`). Passing scope as a parameter would not add any enforcement — the adapter would still use the key to address the bucket, and an incorrect key is caught by the missing-object error, not by the type checker. Documented explicitly in the module docstring. |
| A-163 | choice | `ObservabilityPort.child` returns `ObservabilityPort` and binds values to every subsequent event | A use case calls `child(trace_id=..., request_id=...)` once at entry, then passes the child context to every stage. Stages call `emit` without knowing the trace ID. The alternative — threading `trace_id` through every emit call — causes every caller to manage a value it doesn't own. |
| A-164 | choice | `ObservabilityPort` methods are synchronous | Logging must not introduce `await` points inside a pipeline stage. structlog (D-26) is synchronous; an async port would force every implementation to use `asyncio.to_thread` for a library that doesn't need it. |
| A-165 | choice | `RerankerPort.rerank` returns scores in the same order as `candidates`, with no normalisation | The domain does not commit to a score range. Infrastructure determines the scale; calling code uses relative ordering. A normalised interface would hide implementation details that matter for debugging score distributions (A-047). |

## Step 1.9 — Model gateway port

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-166 | choice | `ModelProfile` placed in `model_gateway.py` alongside the protocols that reference it, not in `models/entities.py` | `ModelRequest` and `ModelResponse` are request/response data assembled and consumed per call. `ModelProfile` is static capability metadata about a provider. The two concerns are not naturally co-located; placing the profile next to the gateway port that uses it keeps the gateway's complete contract in one file. |
| A-167 | choice | Three protocol classes (`TextGenerationCapability`, `MultimodalCapability`, `ModelGatewayPort`) rather than one flat gateway protocol with all methods | Provider adapters implement one of the two narrow capability interfaces. The gateway implementation holds a registry of those adapters and itself implements `ModelGatewayPort`. Test stubs for individual providers satisfy a narrower protocol without implementing the full gateway contract. |
| A-168 | choice | `EmbeddingPort` and `RerankerPort` from `adapters.py` serve as the embedding and reranking capability interfaces; no parallel definitions added here | The plan names four capability interfaces; embeddings and reranking are pure-computation concerns with no private content and no privacy pre-flight. A second set of definitions in `model_gateway.py` would duplicate them with no additional constraint. |
| A-169 | choice | `ModelGatewayPort.profile_for` is synchronous | The capability registry is an in-memory mapping built at startup; the method is a dictionary lookup with no IO. A sync method avoids introducing unnecessary `await` points for code that may call `profile_for` in a guard check before building a large prompt. |
| A-170 | choice | The invariant `VISUAL_QUESTION in tasks → supports_images=True` is one-directional | A model may support image input for purposes other than VISUAL_QUESTION, so the reverse (`supports_images → VISUAL_QUESTION in tasks`) is not enforced. The enforced direction is the load-bearing one: a gateway that routes VISUAL_QUESTION to a provider without `supports_images=True` would fail at the provider call. |
| A-171 | discovery | `pytest-asyncio` was listed as a dev dependency but not installed, causing `--strict-config` to block all test runs with exit code 4 | Installed during this step. All 478 tests pass after installation. The root cause was a missing `pip install -e ".[dev]"` in this environment. |

## Step 1.10 — Composition root and wiring

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-172 | choice | `UseCase[_Req_contra, _Res_co]` Protocol placed in `app/application/use_case.py` to make the boundary test non-vacuous | The boundary test scans all non-empty files in `app/application/`. Before this step, all files there were empty `__init__.py` files, so the test passed vacuously. A `UseCase` Protocol is the natural first application-layer abstraction — all future command and query handlers will implement it. |
| A-173 | choice | TypeVars in `UseCase` use correct variance (`contravariant` for request, `covariant` for response) | mypy strict mode enforces variance in Protocol generic parameters. Invariant TypeVars produced a `[misc]` error in strict mode. The variance is also semantically correct: `execute` is a function type whose input is contravariant and output is covariant. |
| A-174 | choice | `Container` is a frozen dataclass with all 18 port fields typed to their Protocol types | A frozen dataclass gives named, typed access to every port without a mutable global. Request handlers receive the container through FastAPI state, not through module-level imports, which keeps the dependency graph explicit and the composition root the only place that chooses adapters. |
| A-175 | choice | `_Unimplemented` sentinel in `wire.py` raises `NotImplementedError` with the port name on `__getattr__`, not at `build_container` time | A startup crash would break the `test_lifespan_runs` test and make the app unusable for all endpoints that do not yet need a database. The sentinel approach lets the skeleton app start and serve the health endpoint; only requests that actually call a port method encounter the error. |
| A-176 | choice | `build_container` accepts `settings: Settings` now even though it is unused, suppressed with `# noqa: ARG001` | Callsites write `build_container(get_settings())`. Adding `settings` after the fact would require every callsite to change. The `# noqa` is local and documents why the argument exists. |

## Step 2.1 — Alembic setup and extension activation

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-177 | deviation | `psycopg` (psycopg3) used as the async driver, not `asyncpg` | The step description said "asyncpg driver" but `pyproject.toml` already specifies `psycopg[binary,pool]>=3.2`. `psycopg` is the modern psycopg3 package; SQLAlchemy 2.x uses it with `postgresql+psycopg://` for both sync and async engines. No additional dependency required. |
| A-178 | choice | `DATABASE_URL` read from `Settings().database.url.get_secret_value()` in `env.py`, not from `alembic.ini` | Credentials in ini files end up on disk and in log output. Reading from application settings keeps the URL in the `SecretStr` wrapper and applies all the same environment-variable resolution that the app uses. |
| A-179 | choice | `downgrade()` in `0001_activate_extensions.py` is a deliberate no-op | Dropping extensions while vector columns, rum indexes, and pg_cron jobs from later migrations still reference them would cascade-drop all those objects. The objects that use the extensions are cleaned up by their own migrations on downgrade. |
| A-180 | choice | Sequential numeric revision `0001` instead of alembic's default hex hash | Human-readable ordering across 20+ migrations. No `alembic revision --autogenerate` chains exist yet, so there is no collision risk from a fixed string. |
| A-181 | assumption | `rum` and `pg_cron` extensions must be enabled in Supabase before the migration runs | `vector` and `pg_trgm` are available by default on Supabase. `rum` and `pg_cron` may need explicit activation in the Supabase Dashboard → Database → Extensions panel. Unverified until the user runs `alembic upgrade head` against their project. |
| A-182 | choice | `known-third-party = ["alembic"]` added to ruff isort config in `pyproject.toml` | The `alembic/` directory at the repo root causes ruff/isort to treat the `alembic` package as first-party. The addition tells ruff to treat it as third-party (same group as `sqlalchemy`) so imports are sorted correctly in `alembic/env.py`. |

## Step 2.2 — Knowledge Base, Document & Page models

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-183 | choice | `test_metadata_starts_empty` removed from `test_alembic_setup.py` | `Base.metadata` is a process-level singleton. Importing the models package (which this step does, in the new test file) registers all three tables with it. Once any model is imported in the same pytest process, the assertion `len(Base.metadata.tables) == 0` becomes permanently false regardless of test ordering. |
| A-184 | choice | Table named `document_pages`, not `pages` | `pages` is too generic — the database will eventually contain graph entity pages, memory pages, etc. `document_pages` is unambiguous and self-describing in queries and FK targets. |
| A-185 | choice | `DocumentElement` model deferred to step 2.3 | Per plan: `document_elements` belongs with the chunk models because elements are the source units that chunks are built from; they land together with `chunks`, the pgvector column, and the tsvector trigger. |
| A-186 | choice | `import app.infrastructure.database.models` placed before `from app.*` in `alembic/env.py` | isort places bare `import` statements before `from` statements within the same package group. Placing it after would trigger ruff I001. Functionality is identical — Python caches modules, so the three first-party imports load each module at most once regardless of order. |
| A-187 | choice | `DocumentPageModel.knowledge_base_id` carries no FK constraint | The column is a denormalized scope field for row-level security and index coverage. The cascade from deleting a knowledge base is handled via `documents.knowledge_base_id → knowledge_bases.id ON DELETE CASCADE` followed by `document_pages.document_id → documents.id ON DELETE CASCADE`. An additional FK would create a redundant constraint and complicate bulk-load ordering. |
| A-188 | mypy fix | `isinstance` narrowing added before `.length` and `.timezone` attribute access in tests | `Column.type` is typed as `TypeEngine[Any]`, which has no `.length` or `.timezone` attributes. An `isinstance(col_type, sa.String)` / `isinstance(col_type, sa.DateTime)` check narrows the type so mypy can verify the attribute exists. |

## Step 2.3 — Chunk models, embedding column, tsvector column, versioning columns

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-189 | choice | `DocumentElementModel` placed in `chunk.py` alongside `ChunkModel` | Both are produced by the ingestion pipeline in the same step; they reference each other via FK (`source_element_id`, `chunk_elements`), so keeping them in one file avoids a circular-import problem between model files. |
| A-190 | choice | `VECTOR(384)` dimension hardcoded at schema level; `embedding_dimension` column records the actual per-row dimension | The schema dimension is fixed at migration time and changes only on a reindex migration. The per-row column records what was actually embedded, for migration-period coexistence where some rows were embedded with the old model and some with the new. |
| A-191 | choice | `chunks.tsv` is `TSVECTOR` maintained by the `chunks_tsv_update` trigger, not a PostgreSQL generated column | Trigger approach allows combining multiple columns with different language weights later (e.g., weighting heading text higher than body text) without a migration. Generated columns (`GENERATED ALWAYS AS`) cannot use dynamic configuration or combine multiple source columns with different weights. |
| A-192 | choice | `chunks.parent_chunk_id → chunks.id ON DELETE SET NULL` (self-referential) | If a parent chunk is deleted in isolation (unusual), its children survive with `parent_chunk_id = NULL` rather than being cascade-deleted. Normal deletion flows go through the document cascade, which deletes all chunks regardless. |
| A-193 | choice | `chunks.source_element_id → document_elements.id ON DELETE SET NULL` | The chunk can outlive its primary source element reference; the full element set is tracked via `chunk_elements`. Setting to NULL on element deletion preserves the chunk without orphan errors. |
| A-194 | bug fix | `server_default` for `TEXT[]` columns must use `sa.text("'{}'")`  not a plain string `"'{}'"` | Alembic SQL-quotes plain strings in `server_default`, turning `'{}'` into `'''{}'''`. PostgreSQL then parses `'{}'` (with embedded single quotes) as a string value rather than an array literal, which fails with "Array value must start with {". `sa.text()` bypasses the quoting layer and passes the expression through literally. |
| A-195 | bug fix | `import sqlalchemy as sa` added to `chunk.py` instead of `from sqlalchemy import text` | The `text: Mapped[str]` column attribute in `DocumentElementModel` and `ChunkModel` shadows the imported `text` function within the class body. Accessing it via `sa.text()` avoids the collision because `sa` (the module alias) is not a column name in either class. |
| A-196 | choice | `heading_path` stored as `TEXT[]`, not JSONB or delimited string | `TEXT[]` maps directly to `tuple[str, ...]` without serialization overhead, supports PostgreSQL array operators if needed, and is more idiomatic than JSONB for a fixed-element-type sequence. |
| A-197 | step | Step 2.4 — retrieval indexes migration `0004_retrieval_indexes.py` | HNSW on `chunks.embedding` (`vector_cosine_ops`, `m=16`, `ef_construction=128`); RUM on `chunks.tsv` (`rum_tsvector_ops`); six composite B-tree scoped indexes on `(user_id, knowledge_base_id, <col>)` for `document_id`, `chunk_type`, `index_version`, `language`, `ordinal`, `content_hash`. 10 unit tests. Migration applied at `0004 (head)`. |
| A-198 | choice | HNSW `m=16, ef_construction=128` | Balanced choice: good recall for text embeddings at student scale (5k–20k chunks), builds in seconds. `m=32` and higher `ef_construction` are reserved for production-scale recall tuning; a `REINDEX` applies them without touching table DDL. |
| A-199 | choice | Six composite index third columns: `document_id`, `chunk_type`, `index_version`, `language`, `ordinal`, `content_hash` | Covers document-scoped listing, modality type filtering, version-pinned retrieval for reindexing, language filtering, sequential ordering queries, and deduplication during re-ingestion. |

## Step 2.5 — Conversation, Message & Memory models

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-200 | step | Step 2.5 — four ORM models in `conversation.py`, migration `0005_conversations_messages_memory.py`, 33 unit tests, `0005 (head)` | `ConversationModel`, `MessageModel`, `ConversationRetrievalChunkModel`, `MemoryFactModel`. |
| A-201 | choice | `messages` model-metadata stored as four separate columns (`model_id`, `prompt_tokens`, `completion_tokens`, `finish_reason`), not a JSONB `model_metadata` blob | The plan described JSONB but the domain entity has individual typed fields. Individual columns are directly queryable (e.g. aggregate token usage), and the domain entity is the authoritative source. |
| A-202 | choice | `memory_facts.provenance` stored as `INTEGER` (the ordinal of the `MemoryProvenance` `IntEnum`) | `MemoryProvenance` is an `IntEnum` where the ordering is load-bearing (higher = more trusted). Storing the integer value makes trust-ordered queries and comparisons native to the column without a lookup table. |
| A-203 | choice | `active_figure_id` and `active_table_id` on `conversations` carry no FK constraint | The visual-element tables (figures, tables) are defined in Phase 6. Adding FK constraints now would introduce a circular dependency on tables that do not yet exist. FKs will be backfilled in Phase 6 if needed, or left as application-enforced references. |
| A-204 | choice | `PLC0415` added to the `tests/**` ruff per-file-ignores | In-method imports are used across all existing test files to control SQLAlchemy mapper registration order — the models register themselves on import, and test isolation sometimes requires controlling when that happens. The rule was already violated in `test_model_gateway.py` and `test_container.py`; adding the ignore resolves the whole class consistently. |

## Step 2.6 — Graph models and traversal indexes

| ID | Kind | Entry | Why |
|---|---|---|---|
| A-205 | step | Step 2.6 — two ORM models in `graph.py`, migration `0006_graph_entities_relationships.py`, 27 unit tests, `0006 (head)` | `GraphEntityModel` and `GraphRelationshipModel`. |
| A-206 | deviation | `canonical_name` (plan) renamed to `name` to match the domain entity | The domain entity uses `name`; the plan's `canonical_name` was a description of the semantics, not the actual field name. The scope+name index still serves as the canonical-name lookup index. |
| A-207 | deviation | `active_graph_version` omitted from `graph_entities` | Only `graph_version` was added, per user decision. `active_graph_version` is a KB-level property (already on `knowledge_bases`); duplicating it on every entity would create a consistency hazard with no retrieval benefit. |
| A-208 | choice | `graph_relationships.source_chunk_id` FK is `ON DELETE CASCADE` | Deleting the source chunk removes the provenance that justified the edge; the edge should not outlive its evidence. `SET NULL` is not available because the column is NOT NULL (provenance invariant). `RESTRICT` would block document-level cascade deletes unnecessarily. |
| A-209 | choice | `user_id`/`knowledge_base_id` denormalized onto `graph_relationships` | The domain entity already carries these fields; storing them directly avoids a join through `graph_entities` in every RLS policy and retrieval query. |

### Step 2.7 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-210 | step | Step 2.7 — two ORM models in `job.py`, migration `0007_job_queue_cache.py`, 26 unit tests, `0007 (head)` | `ProcessingJobModel` and `CacheEntryModel`. |
| A-211 | deviation | `error_message` (plan) renamed to `failure_reason` to match the domain entity | The domain `ProcessingJob` entity uses `failure_reason`; aligning the column name eliminates a translation layer in the repository. |
| A-212 | choice | `scheduled_at` column added to `processing_jobs` | The domain entity carries `scheduled_at`; it enables deferred jobs (queued now, claimable only after a future time) without a schema change. The plan did not list it but it is present in the domain entity. |
| A-213 | choice | `(status, priority)` claim index landed in step 2.7 (not deferred) | The index exists exclusively to support `FOR UPDATE SKIP LOCKED` claim queries; shipping it now with the table keeps DDL and usage rationale co-located. Deferring to a later step would leave the table without its primary access path. |
| A-214 | correction | `postgresql_unlogged` is not a valid SQLAlchemy `Table` kwarg; `__table_args__` removed from `CacheEntryModel` | SQLAlchemy's `_validate_dialect_kwargs` raises `ArgumentError` for `postgresql_unlogged` because the PostgreSQL dialect does not register it as a supported Table-level option. The UNLOGGED flag is a DDL-only concern handled entirely by the migration's raw `CREATE UNLOGGED TABLE` statement. The ORM model carries no `__table_args__`; the docstring is the model-level signal. `test_table_is_declared_unlogged` updated to verify the docstring instead of `table.kwargs`. |

### Step 2.8 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-215 | step | Step 2.8 — RLS policies migration `0008_rls_policies.py`, 16 unit tests, `0008 (head)` | `ENABLE ROW LEVEL SECURITY` + `FOR ALL` policy on all 12 scoped tables. No model file changes. |
| A-216 | choice | `FOR ALL` policy (single policy per table) rather than four operation-specific policies | A single `FOR ALL` with `USING` + `WITH CHECK` is the standard Supabase pattern and achieves the same security with less DDL. Separate per-operation policies are deferred to Phase 3 if more granular control is needed. |
| A-217 | choice | Bridge tables covered by EXISTS subquery to their scoped parent | `chunk_elements` joins through `chunks`; `conversation_retrieval_chunks` joins through `messages`. The parents carry `user_id` so the EXISTS check is equivalent to a direct `user_id = auth.uid()` filter without requiring `user_id` denormalization on the bridge tables. |
| A-218 | choice | `processing_jobs` and `cache_entries` left without RLS | These system tables carry no `user_id` and are not exposed through Supabase's auth layer. They will be locked down with a service-role bypass policy when Phase 3 introduces authentication. |
| A-219 | choice | No `FORCE ROW LEVEL SECURITY` applied | `FORCE ROW LEVEL SECURITY` would also apply policies to the table owner (`postgres`). Since no service-role bypass is wired yet and Phase 3 will add it explicitly, forcing at this stage would lock out legitimate internal operations. |

### Step 2.9 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-220 | deviation | Plan specifies asyncpg driver; psycopg3 async used instead | `asyncpg` is not in `pyproject.toml` dependency groups; `psycopg[binary,pool]` (psycopg3) is installed and fully supports async via `create_async_engine("postgresql+psycopg://...")`. Adding asyncpg would be an unnecessary new dependency. `_normalise_url` converts bare `postgresql://` and `postgres://` to `postgresql+psycopg://`; existing `.env` URL is already in that form. |
| A-221 | choice | `session_factory` added to `Container` dataclass rather than stored separately on `app.state` | Consistent with the existing pattern — everything injected at the composition root lives in the Container. Phase 3 route handlers will read it through a FastAPI dependency without needing to know the engine exists. |
| A-222 | choice | Stub factory placed in `Container.session_factory` when `DATABASE_URL` is absent | `create_async_engine` with an empty URL raises `ArgumentError` immediately. The guard in `build_container` keeps the Container constructable during unit tests that don't configure a database, which matches the existing pattern for unimplemented adapters. |
| A-223 | choice | `ScopedRepository._user_filter` covers the listing case for `knowledge_bases` | `knowledge_bases` has `user_id` but no `knowledge_base_id` column. `_user_filter(KnowledgeBaseModel)` returns `user_id = :user_id`; `_scope_filter` would raise `AttributeError` on that model. Concrete `KnowledgeBaseRepository` methods that need the KB id match it against `id = scope.knowledge_base_id` directly. |
| A-224 | correction | `literal_binds` renders UUIDs without hyphens in SQLAlchemy 2.0 | SQLAlchemy's UUID literal rendering strips hyphens (e.g. `d682bd1f3a04...` not `d682bd1f-3a04-...`). Filter-binding tests now use `str(uuid).replace("-", "")` for comparison. |

### Step 2.10 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-225 | deviation | `upsert_embedding` is not in the `ChunkRepository` protocol — step skipped | The Phase 1 protocol (`repositories.py`) defines only `get`, `save_batch`, `list_for_document`, and `delete_for_document`. The plan step mentioned `upsert_embedding` but it was never added to the Protocol. `save_batch` stores chunks without embeddings; the embedding worker populates them later outside the repository layer. |
| A-226 | choice | Repositories are NOT wired into Container; container holds stubs until Phase 3 | `SqlKnowledgeBaseRepository`, `SqlDocumentRepository`, and `SqlChunkRepository` require both a `ScopeContext` and an `AsyncSession`, which are per-request values. Putting them in the container as singletons would not work. They will be constructed in FastAPI request dependencies (Phase 3) from the container's `session_factory`. |
| A-227 | choice | `repositories/` package under `infrastructure/database/` with one file per repository | Three files (`knowledge_base.py`, `document.py`, `chunk.py`) plus `__init__.py`. Each file defines one concrete class and private mapping helpers (`_to_entity`, `_to_model`). Separates concerns without over-engineering. |
| A-228 | deviation | `DocumentElementModel` and `ChunkModel` tests use `AsyncMock` session, not SQLite | Both models contain PostgreSQL-specific column types (`ARRAY(Text())`, `Vector`, `TSVECTOR`) that aiosqlite cannot create or process. KB, Document, and Page tests use real SQLite; Element and Chunk tests verify SQL compilation and call counts via `AsyncMock`. |
| A-229 | correction | `AsyncSession.expire_all()` is synchronous — `await` raises `TypeError` | `expire_all()` marks objects expired in the identity map without touching the DB; it returns `None`, not a coroutine. Tests call `session.expire_all()` (no `await`). |
| A-230 | correction | SQLite strips timezone from `DateTime(timezone=True)` columns | SQLAlchemy's SQLite dialect does not preserve `tzinfo` on readback. All three repository mapping modules add a private `_utc(dt)` helper that calls `dt.replace(tzinfo=timezone.utc)` when `dt.tzinfo is None`. PostgreSQL always returns tz-aware datetimes so the helper is a no-op there. |
| A-231 | choice | `aiosqlite>=0.20` added to `[dev]` dependency group | Required async SQLite driver for `create_async_engine("sqlite+aiosqlite:///:memory:")` in the repository unit tests. Not a runtime dependency. |
| A-232 | choice | Every scoped method calls `ScopedRepository._require_scope(scope)` before touching the session | The protocol passes a `ScopeContext` per call, but the filters are built from the scope the repository was constructed with. Nothing reconciled the two, so a caller passing a different scope would silently query the bound one — and ruff flagged all 15 `scope` parameters as unused, which is the same fact stated as a lint. `_require_scope` delegates to `ScopeContext.require_ownership`, raising `ScopeViolationError` on any mismatch. The alternative considered and rejected was a per-file `ARG002` ignore, which would have left the parameter decorative. |
| A-233 | correction | `Model.__table__` is typed `FromClause`; `create_all(tables=...)` requires `Table` | Passing `__table__` directly is a type error under both mypy and Pyrefly even though it works at runtime. The `sqlite_session` fixture now looks each table up as `Base.metadata.tables[Model.__tablename__]`, which is typed `Table` and keeps the model imports load-bearing. |
| A-234 | correction | An async fixture is annotated with what it yields, not what it returns | `sqlite_session` was annotated `-> AsyncSession`; because it yields, the correct annotation is `AsyncIterator[AsyncSession]`, matching `get_session` in `session.py`. The wrong annotation failed `pytest_asyncio.fixture`'s overload resolution. |
| A-235 | correction | `Delete` is exported from the `sqlalchemy` namespace | `test_uses_delete_statement` reached it as `sa.sql.dml.Delete`, relying on a submodule another import happened to load. Imported as `from sqlalchemy import Delete` instead. |

### Step 2.11 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-236 | choice | `SqlJobRepository` does not inherit from `ScopedRepository` | Worker methods (`get`, `save`, `claim_next`) have no `ScopeContext`; scoped construction would require it at build time. Plain class with just `AsyncSession` in its constructor. `list_for_scope` filters through JSONB payload access. |
| A-237 | choice | `list_for_scope` filters via `payload["knowledge_base_id"].as_string()` JSONB access | `ProcessingJobModel` has no `user_id` or `knowledge_base_id` columns; the KB id lives in the JSONB payload. SQLAlchemy's JSONB subscript API produces the correct PostgreSQL expression. PostgreSQL-specific; all job tests use `AsyncMock`. |
| A-238 | choice | `claim_next` reads `now` from `datetime.now(UTC)` internally rather than accepting it as a parameter | The protocol does not include a `now` parameter. The method stamps the claim atomically from inside the same transaction. Determinism in tests is achieved by mocking `session.execute` to return a pre-built pending model; the claim timestamp is not asserted. |
| A-239 | choice | `graph_version=1` set explicitly in `_entity_to_model` and `_rel_to_model` | The domain entities carry no `graph_version` field, so the repository cannot preserve the current DB value on update. Bumping the version is responsibility of the extraction pipeline, not the repository. Setting to 1 explicitly is safe for the current phase because no version management is wired yet. |
| A-240 | choice | `GraphRepository.delete_for_document` deletes graph_entities only; relationships cascade | The FK on `graph_relationships.source_entity_id` and `.target_entity_id` both carry `ondelete="CASCADE"`. One DELETE on `graph_entities WHERE source_document_id = ?` is enough; the DB removes orphaned relationships automatically. |
| A-241 | choice | SQLite tests for graph include `source_chunk_id` as a random UUID with no backing chunks row | SQLite does not enforce FK constraints by default (no `PRAGMA foreign_keys = ON`). The graph tables declare FKs to `chunks`, but `chunks` is not in the SQLite fixture because it uses `pgvector.Vector`. Random UUIDs satisfy the NOT NULL constraint without referential integrity. |
| A-242 | deviation | `pgvector` and `aiosqlite` needed to be installed manually before tests ran | Both packages were listed as dependencies (`pgvector>=0.4` in main, `aiosqlite>=0.20` in dev) but were absent from the active environment. Installed via `pip install pgvector aiosqlite` to unblock the test run. |
| A-243 | choice | `sqlite_session` fixture extended in-place rather than creating per-file fixtures | The eight SQLite-compatible tables share FK relationships (conversations → knowledge_bases, messages → conversations, graph_entities → knowledge_bases). One fixture that creates all eight avoids duplicating FK-ordered table lists across files and keeps test file preambles short. |
| A-244 | choice | Integration tests use raw `text()` SQL, not ORM models | ORM models are the source of truth for the application layer. Migrations are the source of truth for the database schema. Using raw SQL in smoke tests verifies the actual schema in the live DB, catching column renames or type changes that autogenerate might not flag. |
| A-245 | choice | `db_session` fixture uses outer-transaction rollback pattern, not savepoints | Each test gets an `AsyncSession` bound to an explicit connection. `flush()` writes SQL within the transaction; `rollback()` in the fixture teardown undoes everything. No savepoints needed because tests only call `flush()`, never `commit()`. |
| A-246 | choice | `test_rls_blocks_anonymous_access` uses a direct connection, not `db_session` | `SET LOCAL ROLE anon` must execute on the same connection object as the INSERT to affect the same transaction. `db_session` wraps a connection opaquely; a raw `engine.connect()` makes the sequencing explicit and guarantees `rollback()` fires even after the role switch. |
| A-247 | deviation | `document_pages` has no `created_at`/`updated_at`; `chunks` has `created_at` but no `updated_at` | Initial smoke test used `(created_at, updated_at)` for both tables. Verified against migration 0002/0003: `document_pages` has neither timestamp column; `chunks` has only `created_at`. Column names corrected to match migration definitions. |
| A-248 | deviation | `chunks.document_id` FK points to `documents`, not `document_pages` | Initial test inserted a `document_pages` row as the FK parent for chunks. Actual migration: `chunks.document_id` → `documents.id`. `document_page_id` does not exist in the schema. Test corrected to insert `documents` as the FK parent. |
| A-249 | choice | `WindowsSelectorEventLoopPolicy` set at integration conftest import time | psycopg3 async cannot use Windows ProactorEventLoop. Setting `WindowsSelectorEventLoopPolicy` at module level in `tests/integration/conftest.py` ensures pytest-asyncio picks up the correct loop before collecting any test in that directory. Unit tests remain unaffected (aiosqlite tolerates ProactorEventLoop). |
| A-250 | choice | Destructive round-trip test uses subprocess alembic, not Python API | `alembic.command.downgrade()` in-process requires the env.py to read `DATABASE_URL` from the subprocess environment. Running alembic as a subprocess with `DATABASE_URL` overridden to `TEST_DATABASE_URL` avoids patching the live Settings instance and mirrors how operators would run the command manually. |

### Step 3.1 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-251 | discovery | `AuthenticationError` was absent from the domain error hierarchy | The step plan assumed it existed; `errors.py` had no auth error. Added `AuthenticationError(DomainError)` before implementing the JWT layer. The domain error hierarchy now covers auth alongside NotFoundError, ScopeViolationError, and the rest. |
| A-252 | choice | RS256 via JWKS, not HS256 with a shared secret | `SupabaseSettings` already had `jwks_url` and `jwks_cache_seconds`, making the JWKS path the consistent choice. The user confirmed RS256. `jwt_secret` / `SUPABASE_JWT_SECRET` is not added. |
| A-253 | choice | `JwksClient` holds one in-process cache; no distributed cache or lock | The app is a modular monolith (D-06) with one API process. A dict keyed by `kid` with a monotonic-time expiry is sufficient. Two simultaneous cache-miss requests may both fetch; that is a harmless double-fetch, not a consistency problem. |
| A-254 | choice | `get_current_user` raises `HTTPException(401)` directly rather than domain `AuthenticationError` | FastAPI dependencies are presentation-layer code; raising `HTTPException` is idiomatic there. The exception-to-HTTP mapping middleware (step 3.3) handles domain errors from route handlers, not from dependencies. |
| A-255 | deviation | Test's `_make_app` initially used `= get_current_user` instead of `Depends(get_current_user)` | FastAPI requires `Depends()` wrapping to recognise a callable as a dependency. Without it every request returns 422 (unresolvable parameter). Fixed by using `Annotated[uuid.UUID, Depends(get_current_user)]` in the test route signature. |

### Step 3.2 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-256 | choice | File placed at `app/api/dependencies/scope.py`, not `app/api/deps/scope.py` | PLAN step table said `deps/` but step 3.1 already established `dependencies/` as the directory. Consistency within the codebase wins over the plan text. |
| A-257 | choice | `get_kb_scope` queries only `id` and `user_id` columns, not the full model | Loading the full `KnowledgeBaseModel` row would waste bandwidth for a check that only needs ownership. `select(KnowledgeBaseModel.id, KnowledgeBaseModel.user_id)` fetches two columns via a lightweight `Row` object. |
| A-258 | choice | Missing and foreign KB raise identical `HTTPException(404, "Knowledge base not found")` | FR-AUTH-13 requires the two cases to be indistinguishable. A single branch `if row is None or row.user_id != user_id` enforces this at the code level — separate branches for the two cases would risk diverging error messages. Verified by `test_missing_and_foreign_responses_are_identical`. |
| A-259 | choice | Tests use `dependency_overrides` with an async-generator override for `get_session` | `get_session` is a yielding dependency. Replacing it with a plain lambda returning the mock would work (FastAPI normalises both) but an async generator makes the override's intent explicit and mirrors the original's shape. |

### Step 9.11 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-260 | choice | Retrieval rows are written with `merge`, not `add` | The composite key is `(message_id, chunk_id)`. Regenerating an answer for the same message would collide on insert; merge overwrites instead. Eight rows at most per answer, written after the last token has already reached the student, so the extra select per row costs nothing anyone waits on. |
| A-261 | choice | The persisted score falls back rerank → fusion → zero | The column is not nullable, and the score exists to explain the stored rank. Reranking decides the final order when it runs, so it is preferred; fusion stands in when the pipeline returned before reranking. Zero is the last resort rather than a null, because a row recording a position with no score behind it is a record nobody can interpret. |
| A-262 | choice | `Evidence` imported into the repository ports module | The conversation port now names a retrieval type. Both are domain, so no dependency rule is bent, and the alternative — a parallel record type carrying chunk id, rank and score — would be the same data under a second name, kept in sync by hand. |
| A-263 | choice | `conversation_retrieval_chunks` added to the SQLite fixture despite its foreign key to `chunks` | The table itself uses only standard column types, so it creates cleanly. `chunks` cannot be created in SQLite because of its vector and tsvector columns, but SQLite does not enforce foreign keys by default, so the reference is accepted and the write path is testable without PostgreSQL. |

### Step 9.12 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-264 | choice | The evidence record is written inside the streaming generator's `finally`, not in `execute` | It hangs off the assistant message, which does not exist until generation ends. A caller that takes the generator and never advances it therefore leaves no record — correct, because no answer was produced for anything to be recorded against. |
| A-265 | choice | Written when generation fails as well as when it succeeds | The evidence reached the prompt either way, and a partially streamed answer can still carry a citation. Skipping the failure path would leave exactly the answers most worth auditing with nothing to audit against. |
| A-266 | choice | Empty evidence still calls the repository rather than being short-circuited in the use case | The repository already returns early on an empty sequence. Guarding in both places means two rules that can drift; the use case states what happened and the repository decides what that is worth writing. |
| A-267 | assumption | Insert ordering relies on SQLAlchemy sorting mappers by foreign-key dependency | The retrieval rows carry a foreign key to `messages`, and both writes are pending in the same session with no flush between them. The use case holds a repository port, not a session, so it cannot flush without reaching through the abstraction. SQLAlchemy's unit of work topologically sorts inserts across mappers, which makes the ordering correct — unverified against PostgreSQL until an integration test covers the streaming path. |
| A-268 | discovery | The conversations router never commits its session | `knowledge_bases.py` and `documents.py` call `session.commit()`; `conversations.py` does not, on any of its five endpoints. `get_session` yields a session and closes it without committing. Conversations, messages and now retrieval records are therefore written to the session and discarded when the request ends. Predates this step — message persistence has never actually reached the database. The streaming endpoint is the hard case, because the generator outlives the handler. Needs its own step. |

### Step 9.13 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-269 | choice | The gate uses real `Chunk` and `Evidence` objects rather than mocks | The invariant is that the text placed in the prompt and the id written to the record come from the same chunk. A `MagicMock` satisfies both attribute reads independently, so the two could drift apart and the test would still pass — it would assert the shape of the code rather than the property. |
| A-270 | choice | Equality asserted in both directions, not containment | A missing row makes a genuine citation look fabricated; a spurious row makes a fabricated citation look genuine. Only the second is dangerous, but a containment assertion in either direction alone leaves the other unguarded, so the set comparison is exact. |
| A-271 | choice | The gate was verified by mutation rather than by passing on first run | Three deliberate breakages were introduced and the suite rerun: truncating the record to one item (5 tests failed), recording chunks the model never saw (4 failed), and skipping the write on the failure path (1 failed). A gate test that has never been observed failing is an assumption, not a gate. |
| A-272 | discovery | `test_measures_real_elapsed_time` is flaky, failing roughly one run in three | `StageTimer` is asserted to report at least 20 ms after a `time.sleep(0.02)`. Windows timer granularity makes the measured span land just under the sleep duration often enough to fail intermittently. Unrelated to this step; the assertion needs a tolerance or a monotonic floor. |
| A-273 | deviation | The gate proves the use case hands the correct set to the repository, not that the set reaches PostgreSQL | The conversations router still does not commit (A-268), so nothing this suite covers is durable in production. The gate is written at the seam that exists today; it will need an integration-level counterpart once the transaction boundary is settled. Recorded so the suite is not mistaken for end-to-end proof. |

### Step 9.14 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-274 | correction | `create_conversation` now commits; it was the only non-streaming conversation endpoint that wrote | The other three read. A 201 carrying an identifier for a row that was rolled back on the way out is worse than a failure, because the client stores the id and every later request against it returns 404. Audited every router afterwards: knowledge bases commits on all three writes, documents on both, the remaining routers are 501 stubs with no writes. The streaming endpoint is the one gap left. |
| A-275 | choice | Explicit commit in the handler rather than commit-on-teardown in `get_session` | Making the session dependency commit on clean exit would fix every endpoint at once, including ones not yet written, but it would also commit partial work whenever a handler returned early after a write it did not intend to keep. The codebase already commits explicitly in two routers; consistency with that beats a global change to transaction semantics made as a side effect of a bug fix. |
| A-276 | choice | A test asserts the commit follows the write, not merely that both happened | Committing before the write would produce an empty transaction and a response that still looks correct. Order is the property that matters, so it is the property asserted. Verified by removing the commit and confirming two tests fail. |
| A-277 | correction | `_event_stream` given its missing return annotation | Two long-standing mypy errors in the file, unrelated to this step but in code being touched. `AsyncIterator[str]` clears both and leaves the module clean under the type checker. |

### Step 9.15 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-278 | choice | `ConversationUnitOfWork` added as a port; `AnswerUseCase` receives it in place of a repository | A repository handed in ready-made borrows whoever built it, and for a request-scoped session that means the writes are durable only while the request lasts. Producing one per unit of work moves the transaction boundary to where the work is. The port is a callable returning an async context manager, which `contextlib` types from the standard library, so the domain still imports nothing else. |
| A-279 | choice | One turn is two transactions, not one | The question is committed as it arrives and the answer in a second block after the stream ends. They are separated by the end of the request, so they could not share a session even if that were desirable. It also gives the better failure behaviour: a question that was asked stays recorded when generation fails. |
| A-280 | choice | The unit of work opens its own session from the application-scoped factory | The engine outlives every request, so a session drawn from it after the handler returns is valid. This is the same shape the ingestion worker uses. Rejected the alternative of committing inside the router's event-stream wrapper, which depends on FastAPI's teardown ordering for yielding dependencies under a streaming response — behaviour that is version-sensitive and would fail silently by writing into a closed session. |
| A-281 | choice | Retrieval still runs on the request's session | It completes before the response starts streaming and only reads, so it has no stake in the new boundary. Moving it would have meant a transaction held open across model calls for no benefit. |
| A-282 | discovery | Every existing test of this path was blind to the bug | All of them assert against a mocked repository, which is exactly the layer at which a missing commit cannot be seen — the use case calls `save`, the mock records it, and nothing asks whether a row exists. Added `tests/integration/test_answer_persistence.py`, which runs a turn on one connection and reads the rows back on a second one opened afterwards. |
| A-283 | deviation | The integration test is written but has not been run | `TEST_DATABASE_URL` is unset locally and the five tests skip. It also commits and then deletes rows, so it needs a deliberate decision to point at a real database rather than being run in passing. Unverified until then; the unit-level coverage of the boundary is verified. |
| A-284 | choice | The two-transaction property is asserted by counting blocks opened, not by inspecting sessions | The test's unit of work appends to a list each time it is entered, so the assertion is on the number of transactions the use case opened. Verified by collapsing the code back to a single shared repository, which fails two tests. |

### Documentation reconciliation entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-285 | correction | `PLAN.md` reconciled against the codebase after drifting seven phases | The status block read "Phase 2 complete — Phase 3 next" while phases 3 and 9 were built. Ticked 45 verified checkboxes, added a status line to each of the eight partially-built phases naming its specific gaps, and gave phase 9 a step table. Also recorded that the numbering no longer describes the build order, since phase 9 was completed while 5, 6 and 8 were untouched. |
| A-286 | discovery | Phase 1 steps 1.6, 1.7 and 1.8 were marked not-done despite shipping | Their commits are `f4b2312`, `58d0576` and `5849141`. The status block directly above the table asserted "10 of 10", so the document contradicted itself in two places and neither was noticed. |
| A-287 | correction | Plan steps 3.1 and 3.2 described code that was never written | 3.1 specified HS256 with a shared secret; RS256 via JWKS was built. Both named `app/api/deps/`, which became `app/api/dependencies/`. Corrected the text rather than ticking a description of something else — a ticked box against wrong prose is worse than an unticked one. |
| A-288 | discovery | The "Inputs needed" table was stale in both directions and missing an entry | Supabase and R2 credentials are supplied but were marked absent. Ollama is genuinely absent — nothing responds on `localhost:11434`, which means no test in the repository has ever made a real model call, since every test that touches the gateway uses a fake. `TEST_DATABASE_URL` was never listed at all, which is why the integration suite skips silently. |
| A-289 | choice | Transaction boundaries documented as `ARCHITECTURE.md` §5.4 rather than folded into §6 | §6 is about what is stored and where; this is about when a write becomes durable, which is a data-flow property. Placed after scope enforcement, since both are cross-cutting guarantees rather than stages. `ConversationUnitOfWork` added to the ports table. |
| A-290 | correction | The integration seed named `documents` columns that do not exist | Written as `size_bytes` and `processing_status`; the schema has `byte_size` and `status`. Raw SQL was chosen deliberately so the test checks the real schema rather than the ORM's view of it, which only works if the column names are read from the migration instead of assumed. Fixed in `0c7dea1`. |

### Step 5.1 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-291 | choice | Four signals, all normalised to a share of the page rather than absolute | `native_character_count`, `text_area_ratio`, `image_area_ratio`, `vector_drawing_count`. Each maps to something `pdfplumber` can measure cheaply, and ratios rather than areas mean one set of thresholds holds for any page size. Absolute areas would have needed recalibrating per paper size. |
| A-292 | choice | A usable text layer requires both a character minimum and a coverage minimum | Either test alone is fooled in a way the other catches. Some scanners emit a text layer covering the page that holds nothing, which coverage accepts; a dense caption under a full-page image holds plenty of characters across almost no area, which a character count accepts. Both mutations are covered by tests that fail when the guard is removed. |
| A-293 | choice | `COMPLEX` is evaluated first, before the text layer is considered at all | Dense vector line work is difficult regardless of how good the text layer looks, because the layer holds the labels and none of the structure joining them. A schematic with perfect embedded text still reads as a bag of words in roughly the wrong order. |
| A-294 | choice | A blank page classifies as `NATIVE_TEXT`, not `SCANNED` | It has no images, so nothing triggers the scanned rule, and the fallthrough is deliberate rather than incidental: sending a blank page for recognition spends the expensive path to confirm it is still blank. |
| A-295 | choice | Thresholds added to `OcrSettings` rather than a new settings group | `native_text_coverage_threshold` was already there and is exactly a classification threshold, so the alternative was splitting one decision across two groups. The classifier takes the four values as constructor arguments rather than a settings object, because the domain must not import settings — the same shape `RetrievalOrchestrator` already uses. |
| A-296 | assumption | `complex_vector_drawing_threshold` defaults to 400 with no data behind it | A prose page draws almost nothing, a ruled table draws tens, a schematic draws thousands. Where between those the vision-language path starts paying for itself is unknown until real material is classified, which is precisely what the reassessment point after 5.6 is for. Recorded in the open assumptions table alongside A-098. |
| A-297 | choice | Tests pin thresholds with fixed local values, not the configured defaults | The defaults are calibration values expected to move. A test reading them would move with them and stop testing the rule. Every threshold is asserted at the value exactly, one step below and one step above, and an off-by-one on the vector comparison fails three tests. |

### Step 5.2 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-298 | choice | The whole parse runs on a worker thread via `asyncio.to_thread` | pdfplumber is synchronous and CPU-bound, and the worker holds its job lease with a heartbeat on the same event loop. Parsing on that loop would stall the heartbeat and let a lease expire while the work it covers is still running — the job would then be reclaimed and processed twice. Covered by a test that runs a ticker task concurrently with a parse and asserts it advanced. |
| A-299 | discovery | `extract_text_lines()` reports no `y0`/`y1`, only `top`/`bottom` measured downwards | The first implementation assumed bottom-origin values were available and passed them through, which put every bounding box on the wrong half of the page while still producing plausible-looking numbers. Boxes are now flipped against the page height, which also swaps which value is which: a line's `top` is the smaller number going down and becomes the larger `y1` going up. Removing the flip fails the golden file and an explicit origin test. |
| A-300 | choice | Paragraph gaps are measured against the smaller of the two lines they separate | The space under a heading is modest relative to the heading and generous relative to the body beneath it, and it is a paragraph break either way. Taking the larger line as the reference swallows every heading into the paragraph below it — verified by mutation, which fails three tests. |
| A-301 | correction | The first fixture was too sparse to exercise the path it was meant to cover | Six short lines give a text-area ratio under 3%, so the page classified by falling through the rules rather than by satisfying the one about a usable text layer. Rewritten to a realistic density — full measure, about thirty lines — which lands at 16.5% against a 10% threshold. A fixture that passes without reaching the behaviour under test is worse than no fixture. |
| A-302 | discovery | An ASCII apostrophe comes back out of extraction as U+2019 | Helvetica's StandardEncoding maps byte `0x27` to `quoteright`, not to the typewriter apostrophe, so the golden file records the curly form. This is the PDF being read correctly rather than the parser being wrong. The fixture deliberately keeps one apostrophe so the behaviour stays covered, and `make_fixtures.py` explains it for whoever edits the strings next. |
| A-303 | choice | Fixtures are generated by a committed script rather than dropped in as opaque binaries | A PDF cannot be reviewed in a diff. The script writes them by hand in full, so what a fixture contains can be read, and regeneration is byte-for-byte deterministic — verified by hashing before and after — so a rebuild that changes a file means the script changed and not the run. |
| A-304 | choice | Unreadable files raise `UploadValidationError` rather than a new parse error | The upload path already raises it for a PDF it cannot read, and this is the same judgement reached later. The empty-document check sits after the parse rather than inside it, so the broad handler that turns library exceptions into that error is not also where this separate decision is made. |
| A-305 | choice | `ParsingSettings` added as its own group rather than extending `OcrSettings` | Page classification thresholds went into `OcrSettings` in 5.1 because one was already there, but paragraph grouping is not an OCR concern, and 5.4 and 5.5 will add more parsing values. A group now is cheaper than scattering them later. |

### Step 5.3 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-306 | choice | The parser is wired into the container's `pdf_parser` slot rather than built in the worker | The slot has existed since Phase 1 holding an `_Unimplemented` sentinel, and the composition root is where adapters are chosen. The worker now reads `container.pdf_parser` and constructs nothing itself, which also means the API process could parse without a second wiring path being written. |
| A-307 | choice | Pages and elements are persisted before chunking begins | They are what the parse established, and they stay true whether or not the stages after them succeed. A page recorded as needing recognition is worth keeping even if this run then fails on embeddings, because the next run does not have to rediscover it. Asserted by a call-order test. |
| A-308 | correction | Page count now comes from the parse rather than from the document | The old code took `doc.page_count` if set, falling back to the highest page number that happened to yield text — which undercounts any document whose last pages are scanned. The parse counts every page including the ones it could not read, so it is the authority. The upload-time count remains a validation figure, not a record. |
| A-309 | choice | Elements are flattened back to page text for the chunker, joined with a blank line | Chunking deliberately does not change in this step. Rewriting the splitter over structure is worth doing once, after the parser can distinguish a heading from a paragraph, and doing it now would mean writing it twice. Joining paragraphs with a blank line rather than a space carries the boundaries forward for the splitter that will eventually use them. |
| A-310 | correction | The "empty document" tests were modelling a state the parser cannot produce | They stubbed a parse returning no pages, which now raises before the use case sees it, and `mark_completed` rejects a page count of zero. Rewritten around the case that does occur and that the step exists to handle: pages that exist and yielded no text, which is what a scanned document looks like before recognition. |
| A-311 | correction | `test_every_slot_raises_not_implemented_on_access` rewritten rather than left failing | It asserted `model_gateway` was unwired, which stopped being true at `897a88d`, and wiring `pdf_parser` here would have made it wrong twice over. Replaced with two parametrised lists — slots that hold a real adapter, and slots that still name themselves when used. Moving a slot between them is now a deliberate edit rather than a deletion. |
| A-312 | discovery | Re-ingesting a document would duplicate its pages, elements and chunks | `save_pages` and `save_elements` merge on primary key, but the parser mints new identifiers on every run, so a second pass inserts a parallel set rather than replacing the first. Chunks have had the same property since Phase 4. Nothing triggers reprocessing today, but `COMPLETED → PROCESSING` is a permitted transition (A-091) precisely so that reindexing is possible, so this needs a deletion step before that is used. Not fixed here: a partial fix covering pages but not chunks would look complete and not be. |

### Step 5.4 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-313 | choice | Element typing is a domain rule, matching the page classifier | `ElementClassifier` takes measured signals and returns a type. The parser gathers what only it can see — type size, weight, line count — and the classifier derives everything from the text itself, so rules about wording live in one place rather than being half-computed by whoever collected the signals. |
| A-314 | choice | Every size judgement is relative to the page's own body text | A sixteen-point line is a heading on a page of ten-point prose and is ordinary text on a page set entirely in sixteen. The body size is the median character size on the page, so one large title does not drag the reference upward. Replacing the ratio with an absolute size fails four tests. |
| A-315 | choice | Text-reading rules run before appearance-reading ones | Captions are frequently bold and short, and list items are short, so both would be claimed as headings if the appearance rules ran first. Ordering the rules is the policy, not an implementation detail. |
| A-316 | choice | A heading must also be short and unpunctuated | Size or weight alone promotes any emphasised sentence inside a paragraph. Requiring brevity and the absence of a sentence ending is what separates a title from emphasis. |
| A-317 | choice | Only genuinely mathematical symbols count toward a formula | Arithmetic operators and brackets are excluded deliberately: ordinary prose is full of hyphens and parentheses, and counting them would turn every parenthetical into an equation. |
| A-318 | choice | Text inside a detected table is excluded from paragraph grouping | pdfplumber reports it as ordinary lines as well, so left in it appears twice — and the flattened copy is the one that mixes rows together and loses which column a number came from. Removing the exclusion fails a test written for exactly that. |
| A-319 | choice | List items keep their own lines; prose rejoins with a space | Prose wraps mid-sentence, so its lines belong together. List items do not wrap, and joining them with a space runs them into one sentence nobody wrote. The type is decided first and the text is rejoined accordingly. |
| A-320 | choice | Figures are recorded with a box and no text | A figure has nothing to say until something looks at it. Writing a description now would put text into the document that the document does not contain; recording it empty is what lets a later stage find it. |
| A-321 | choice | Elements are gathered by kind and then interleaved by position | Text runs, tables and figures are found in three different ways. Sorting all of them at the end keeps a figure between the paragraphs it sits between, rather than appended after every paragraph on the page. |
| A-322 | deviation | Captions are typed from their label, not from proximity to a figure | The step called for both. Proximity needs column detection to avoid associating a caption across a column boundary, and that arrives in 5.5 — so the half that would have been rewritten was left out. Associating a caption with a specific figure is Phase 6's work in any case; this step establishes only that a run of text is a caption. |
| A-323 | choice | Ruff's ambiguous-character warnings suppressed on specific lines, not disabled | The Greek letters and dashes it objects to are the point of the code. Ruff does not read a `noqa` inside a string literal, so the bullet characters were hoisted to their own named constant where the suppression can sit on a real source line. |

### Step 5.5 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-324 | discovery | pdfplumber merges columns into single lines before the parser ever sees them | `extract_text_lines()` groups words by vertical position alone, so on a two-column page the left and right columns arrive already concatenated. No amount of reordering afterwards can repair it — the strings have been joined. Lines are now built here from `extract_words()`, ending a line at any horizontal gap wide enough to be a gutter. On a single-column page no gap is ever that wide and the result matches what the library produced. |
| A-325 | discovery | Spanning content hides the gutter it crosses | A full-width heading or figure physically covers the gap between columns, so measuring horizontal extents naively finds no gutter and reads the page as one column. Boxes conspicuously wider than the median are excluded from that measurement — compared against the median rather than the mean so a handful of full-width items cannot drag the reference up to include themselves. Removing the exclusion fails five tests. |
| A-326 | choice | Spanning elements divide a page into bands rather than being sorted among the columns | Everything above a full-width figure is read before it and everything below after. Without banding, a whole column would be read past the figure and the reader carried back up, which is wrong in a way that reads plausibly. |
| A-327 | choice | Column assignment happens before paragraph grouping, not after | Two columns put their lines at the same heights, so grouping by vertical spacing first weaves them into paragraphs that read across the gutter. Ordering is a separate later concern; grouping is where the damage would already be done. |
| A-328 | choice | A change of type size ends a paragraph block, independently of spacing | A chapter title immediately above a section title sits at ordinary leading, and the gap rule alone joined them into one element carrying both names. The size rule is what separates them, and it holds however generous the spacing tolerance is. |
| A-329 | choice | Heading levels are inferred from type size, and the stack is document-level | Nothing in a PDF states a heading's level. A heading closes every open section set at its own size or smaller and nests under any set larger. The stack crosses page boundaries deliberately: a section beginning near the foot of one page continues onto the next, and per-page state would lose it at exactly the break where content has the least context of its own. |
| A-330 | correction | The first two-column fixture had columns too narrow to be columns | Its lines ran 81 points against a 90-point minimum column width, so the gutter was correctly rejected as producing a sliver and the fixture tested only that guard. Rewritten with columns of realistic measure. A second attempt then overran the gutter entirely — the lines were long enough to reach into the right column's position — which is the same class of mistake in the other direction. |
| A-331 | choice | Font signals now come from per-word attributes rather than character dicts | Lines are assembled here rather than by the library, so they no longer carry the library's character lists. `extract_words(extra_attrs=["size", "fontname"])` supplies the same information at the granularity the new line builder works in. |

### Step 5.6 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-332 | choice | Renders go to the cache store, never to permanent storage | A render is reproducible from the original at any time, so keeping it permanently multiplies what a document costs to store for no lasting gain. The cache adapter already applies the render prefix and enforces its own time-to-live, so the renderer supplies only a key and a lifetime. |
| A-333 | choice | The cache key is derived entirely from what the render is of | Scope, document and page number, and nothing else. That is what makes asking twice cheap rather than merely correct, and what makes a re-render replace its predecessor instead of accumulating beside it. Making the key unstable fails six tests, including both discard tests — deletion targets the same key rendering used, so an unstable key silently leaks every render it ever made. |
| A-334 | choice | The key is scoped like every other stored thing | A cache is still a place data sits. One user's renders should not be addressable by another even by accident, and the scope in the key is what ensures that without relying on the cache adapter to enforce anything. |
| A-335 | choice | Rendering runs on a worker thread | Same reason as parsing: pypdfium2 is synchronous, drawing a page at recognition resolution is slow, and the worker holds its job lease with a heartbeat on the same event loop. Covered by a test that runs a ticker concurrently with a render. |
| A-336 | choice | No domain port for rendering yet | Nothing in the application layer renders a page — the recognition stage that will is deferred. A port invented before its caller exists would be a guess at the signature that caller wants, which is the same reasoning that deferred the evidence-record reader in 9.11. The renderer sits in the container as infrastructure until then. |
| A-337 | correction | Document loading was outside the error guard | A file that could not be opened at all raised the library's own error rather than the domain one, so a malformed upload would have surfaced as an unhandled `PdfiumError` from a worker thread. Loading is now guarded separately from drawing, and the page-range check sits between them so a page that was never there is reported as such rather than as a drawing failure. |

### Step 4.9 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-338 | choice | `fail()` computes the retry time and sets `scheduled_at`, rather than a caller doing it | The delay is a rule about failure, so it belongs with failure. `claim_next` already filtered on `scheduled_at`, so the field the retry needed was there and unused — the queue was half-built for this and never finished. |
| A-339 | choice | Backoff bounds are parameters, not constants in the domain | They are calibration values, and `JobSettings` has carried `backoff_base_seconds` and `backoff_max_seconds` unused since Phase 0. A domain that hardcoded them could not be recalibrated, which is the same reasoning as the classifier thresholds. |
| A-340 | choice | Backoff has no jitter | One worker processes one job at a time, so there is no thundering herd to spread out. A predictable delay is much easier to reason about when reading why a job ran when it did, and jitter can be added when concurrent workers make it earn its place. |
| A-341 | choice | Retries are found by the same query that finds new work | `claim_next` selects `PENDING` jobs and `FAILED` ones whose delay has elapsed and whose attempts remain, requeueing then claiming inside one transaction. No scheduler process, no second poll loop, and no window in which a job is due but nothing has noticed. `FAILED` stays observable between attempts rather than being skipped through. |
| A-342 | discovery | Retrying a document crashed on an illegal transition | `PROCESSING → PROCESSING` is not a permitted document transition, and the worker marked the document processing at the start of every attempt. Since a document now stays `PROCESSING` between attempts, the second attempt would have raised before doing any work. The worker skips the mark when nothing would change. |
| A-343 | choice | A document is failed only when its job dead-letters | While a retry is pending the document is still being processed, and reporting a failure that is about to be undone would make the status mean nothing. Covered by tests that drive `_run_job` to failure and assert what was saved. |
| A-344 | deviation | `worker_id` remains unused in `claim_next`, and the lint about it is left standing | Recording which worker holds a lease needs a column and therefore a migration. It belongs in 4.10, where reclaiming an orphaned lease is the thing that makes knowing who dropped it worth having. Left visible rather than silenced. |
| A-345 | assumption | Retry pickup is proven by two partial tests rather than one whole one | The repository tests mock the session, so a test that a failed row is requeued and claimed does not also prove the query would have found it. The query is covered separately by reading its compiled parameters. Removing the retry clause is caught, but by one test rather than several — a database-backed test would cover both at once, and belongs with the integration suite that already cannot run. |

### Step 4.10 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-346 | deviation | Reclaim is its own sweep, not part of `claim_next` | The plan said `claim_next` would also consider expired leases. It does not, because a reclaimed job is not immediately claimable — it has just failed, so it waits out a backoff like any other failure — and a method called `claim_next` that wrote to the database and then returned nothing would be a surprising thing to read. `reclaim_expired` sweeps, the existing retry path picks the jobs back up, and the worker calls the sweep once per poll before claiming. The cost is one extra query per poll and a window of at most one poll interval before a lapsed lease is noticed. |
| A-347 | choice | A reclaimed job waits out a backoff rather than being retried at once | The job may be what killed the worker. A malformed file that exhausts memory would otherwise be picked up again immediately and reproduce the crash as fast as the machine allows, which is a crash loop rather than a retry. |
| A-348 | choice | The lapse is recorded as a failed attempt, not a fresh start | It was an attempt and it consumed one. Counting it is what stops a job that reliably kills whatever picks it up from being retried for ever while always looking untried — after enough crashes it dead-letters, which is the point. |
| A-349 | discovery | `str(statement)` silently drops `SKIP LOCKED` | The default dialect renders `FOR UPDATE` and omits the PostgreSQL-specific clause, so a test asserting on the rendered string would have passed against a query that never had it. That assertion now compiles against the PostgreSQL dialect explicitly. |
| A-350 | deviation | `worker_id` is still unused, and the lint about it still stands | A-344 said this step was where recording the lease holder would earn its keep. On reflection it does not: with a single worker there is nobody else it could have been, and a column plus a migration is real work whose only payoff is diagnostics that nothing currently reads. Deferred again, with a better reason than last time — it belongs with whatever first runs more than one worker. |

### Step 4.11 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-351 | discovery | The deletion job could not have been processed even once it was claimed | Its payload carried only a document id and a storage key — no user or Knowledge Base — so a worker had no way to build a scoped repository or to address the cached renders. The enqueue site now includes the scope, matching what ingestion jobs already carried. The bug was not merely that nothing consumed these jobs; a consumer would have failed. |
| A-352 | discovery | Failing a deletion job would have raised on an illegal transition | The worker marked a document `FAILED` when its job dead-lettered, guarded by `is_terminal` — which covers `COMPLETED` and `FAILED` but not `DELETING`. `DELETING` is absorbing, so the transition would have raised inside the failure handler. Now only ingestion failures touch the document, and the guard asks whether the transition is legal rather than whether the state is terminal. |
| A-353 | choice | `PageRendererPort` added now rather than in 5.6 | 5.6 deferred it because no application-layer caller existed and a port invented ahead of its caller is a guess at the signature that caller wants. The caller now exists, and it wanted something the renderer did not have — forget a whole document rather than one page — which is exactly the shape that would have been guessed wrong. |
| A-354 | choice | Renders are discarded before the row is deleted | Renders are addressed per page and the page count lives on the row. Delete the row first and nothing knows how many cached images to look for, so every one of them sits in the cache until its lifetime runs out, belonging to nothing. Reversing the order fails two tests. |
| A-355 | choice | A document that is already gone completes the job rather than failing it | It is the outcome the job exists to produce, and it happens whenever a job is retried after succeeding or the Knowledge Base was deleted first. Failing would retry until the job dead-lettered over work that was already done. |
| A-356 | choice | Deletion runs without a heartbeat | Removing a file, a handful of cache keys and a row is short work. Ingestion holds its lease open with a heartbeat because it can run for minutes; borrowing that machinery here would add a background task and a second transaction for work that finishes in one. |
| A-357 | correction | A test helper could not tell "not supplied" from "explicitly absent" | `_use_case(document=None)` fell through to the default document, so a test meant to exercise the already-deleted path silently exercised the ordinary one. Caught because the assertion it made was specific enough to fail. Replaced with a sentinel. |

### Step 7.1 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-358 | discovery | The ML dependency group is not installed in the active environment | `container.embedder` and `container.reranker` are both `_Unimplemented`, and `sentence_transformers`, `transformers`, `tokenizers` and `tiktoken` were all absent. Every test passes because they use fakes, so nothing had shown it. Ingestion cannot currently produce embeddings here and retrieval cannot rerank. |
| A-359 | choice | The tokenizer is its own dependency in `core`, not part of `ml` | `ml` is deliberately excluded from the default install because it is large. Chunk sizes are specified in tokens, so counting them is not optional, and putting the counter behind an optional group would make the whole of chunking optional with it. The `tokenizers` wheel is small, carries no torch, and loads the same vocabulary the embedding model uses. |
| A-360 | discovery | The character estimate undercounts dense material by more than half | Measured against the real vocabulary: ordinary prose 13 tokens against an estimate of 14, a formula 17 against 8, a table row 15 against 9. So the estimate is close on exactly the text anyone would spot-check and wrong on the text most likely to be dense. A chunk built to an estimated 500 tokens can arrive at the model as nearly 1000, past its 512 limit, and lose its tail — the end of a paragraph, which is usually where it says what it was getting at. |
| A-361 | choice | `TokenCounterPort` is synchronous, unlike every other port | It is a calculation, not a journey anywhere. Making it async would put an await in front of arithmetic and force the chunker — which is otherwise pure and belongs in the domain — to become async purely to reach it. |
| A-362 | choice | Counting excludes the markers the model adds to an input | A passage does not change size by being submitted. What the two special tokens cost is accounted for once, in the headroom `fits` leaves against the model's limit, rather than in every count. |
| A-363 | choice | The counter has no `_Unimplemented` fallback and fails at startup | Every other adapter degrades to a sentinel that raises on first use. This one cannot: a counter that silently estimated would size every chunk wrongly and nothing downstream would show it. Built eagerly so a missing vocabulary is discovered before a job starts rather than halfway through a textbook. |

### Step 7.2 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-364 | choice | The chunker is a domain service returning drafts, not entities | What a chunk *is* — its text, where it came from, what kind of thing it holds — is a question about the document. Its id, its position and which index version produced it are questions about storage. `ChunkDraft` carries the first set and the use case adds the second, which keeps the splitting rules testable without a scope, a clock or a repository. |
| A-365 | choice | Sections are the outer loop, so a chunk cannot span two | Retrieval presents a fragment with the heading path it came from. A chunk straddling a section boundary belongs to two of them and can be honestly labelled with neither. Removing the grouping fails three tests. |
| A-366 | choice | Sections are runs of consecutive elements, not everything sharing a path | A document that returns to a title later — an appendix, a repeated running header — has two runs. Gathering them would build one chunk out of pages that never sat together. |
| A-367 | choice | Overlap carries whole elements rather than a slice of text | A paragraph is the smallest thing worth repeating. Carrying half of one would put a fragment at the head of the next chunk that reads as though it started there. Guarded so that carrying everything forward cannot stall progress. |
| A-368 | choice | The sentence splitter is deliberately conservative | It requires terminal punctuation followed by whitespace and a capital or digit, so it declines to split "Fig. 4 shows" or "0.91 accuracy". A missed boundary only makes a chunk longer; a wrong one cuts a sentence in half, and half a sentence is retrievable by nothing. |
| A-369 | choice | An oversized table splits on its rows, and an oversized part is emitted whole | Rows are the only boundary that does not cut a row in half. Where a single part still exceeds the ceiling it is emitted anyway: one oversized chunk that says something beats two that each say half. |
| A-370 | choice | A bounding box is merged only across elements on one page | A rectangle spanning two pages exists on neither, and a citation opening at it would land nowhere. Chunks that cross a page break carry no box rather than a wrong one. |
| A-371 | correction | Chunks now follow sections rather than pages, and a test asserted the old behaviour | `test_multi_page_document_produces_one_chunk_per_page` encoded the retired splitter's habit of cutting at every page break. A section continuing onto the next page is one passage, so the test now asserts that and checks the chunk spans both page numbers. |
| A-372 | discovery | A mutation check silently did not run because a lint failure short-circuited the chain | The mutation was chained behind `ruff check` with `&&`; ruff failed on an import-sort nit, so the mutation never applied and the suite passed against unmodified code, which read as the tests failing to catch it. Verification steps are now run as their own commands rather than gated behind a check that can fail for unrelated reasons. |

### Step 7.3 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-373 | choice | A parent is a run of whole elements, and its children are cut from that same run | The alternative was to build children first and gather them into parents, but children overlap, so a parent assembled from their texts would repeat the overlap. Grouping elements first makes containment structural: every child comes from the elements its parent was joined from, so a parent always holds its children. Verified by a test that normalises whitespace and checks each child appears inside its parent. |
| A-374 | choice | Overlap stops at a parent boundary | A child carrying trailing text from the previous parent would not be contained by its own, and expansion would then return a passage the fragment is missing from — the one thing expansion exists to prevent. The cost is that the sentence at a parent boundary is reachable from one side only, which is the smaller loss: it happens once per parent rather than once per child. |
| A-375 | choice | Every section yields a parent, even one shorter than the target | A section of two lines produces a parent that duplicates its only child. Storing it anyway means every child has somewhere to expand to, and nothing downstream needs a branch for the fragments that do not. A section short enough to duplicate is short enough that the duplicate costs little. |
| A-376 | choice | Only children are embedded | A parent is what a match expands into, not something to match against. Embedding it would put a section and a paragraph inside that section into the same ranking, competing for the same candidate slots and returning the same content twice. Removing the distinction fails five tests. |
| A-377 | discovery | Parents would have been keyword-searchable the moment they existed | The tsvector trigger builds a vector for every row it is given, so nothing about not embedding a parent kept it out of full-text results. Dense search excluded parents only as a side effect of their embedding being null — a rule that held by accident rather than by statement. Both retrievers now say so outright, and dropping the clause fails a test in each. |
| A-378 | deviation | The retrieval filter was added here, although parent expansion belongs to Phase 10 | This step is what creates parents, so this step is what would have introduced duplicate evidence. Leaving the filter for later would have meant shipping a retrieval regression on purpose and relying on a later phase to remember it. The filter is one condition in each retriever; the expansion that reads parents is still Phase 10's work. |
| A-379 | choice | The two tiers are numbered separately, each from zero | An ordinal says where a chunk sits among the chunks like it. A single sequence across both tiers would mean the chunk after child seven is sometimes a parent, so the neighbours of a passage would depend on how the section above it happened to be divided. `list_for_document` now orders on tier first, since ordering on the number alone would interleave a section with an unrelated paragraph sharing its position. |
| A-380 | choice | The chunker re-checks that a parent target exceeds the child ceiling, which settings already validates | The settings validator protects the configured values; the domain rule protects the chunker from every other caller, including tests. It states the reason a parent exists — a parent no larger than a child restores no context the child did not already carry — where the rule is used rather than only where the numbers are written. |
| A-381 | assumption | Documents ingested before this step are now invisible to keyword and dense search | Their chunks are flat, so every one of them has a null `parent_chunk_id` and reads as a parent. They were produced by the retired character splitter and need re-ingesting regardless, so this is not new work — but until they are re-ingested they will return nothing, and that is a silent emptiness rather than an error. Re-ingestion still duplicates rather than replaces, which A-312 has open. |

### Step 7.4 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-382 | choice | The deliverable is a report to be read, not a test to pass | Every check the parser and chunker have had so far runs against fixtures built to match what was expected of them, which tests consistency rather than correctness: a gutter rule calibrated against a page this repository generated will pass against that page whatever it does to a real one. An assertion here could only encode the same assumptions again. `scripts/inspect_parse.py` prints what actually came out and leaves the judgement to a person holding the document. |
| A-383 | discovery | The report was measuring columns wrongly and saying nothing about it | It asked whether reading order ever jumps back up the page, which is what column detection looks like in most layouts. This resolver works band by band, so a two-column page is read left, right, left, right within each horizontal band and never jumps upward. The report showed zero columns on the two-column fixture, which has two. A diagnostic that is quietly wrong is worse than none, because it will be believed. |
| A-384 | discovery | The corrected rule still had a hole, found by mutation rather than by reading it | Requiring the later block to start at or above the earlier one passed all six tests written for it, and would have missed every page whose left column opens with a heading — which pushes its first paragraph down, so the right column starts higher and the two overlap only partly. Two cases added: a right column starting lower, and a block to the right but wholly below, which is the next band rather than a column. |
| A-385 | choice | The report script carries unit tests, which diagnostic scripts here generally do not | This one decides which tuning numbers move in 7.8, so a wrong reading propagates into configuration and stays. Its two pieces of real logic — what counts as a column, and how page runs are rendered — are pure functions, and both had defects. Loaded by path in the test, following the pattern already used for the environment verifier. |
| A-386 | choice | A file the parser refuses is reported as a sentence rather than a traceback | The zero-page fixture raises `UploadValidationError`, which is the same answer an upload would have received. Printing a stack trace would suggest the tool broke when the tool worked. |
| A-387 | assumption | The assessment half of this step cannot be done yet: there is no real textbook | The repository contains eight hand-built fixture PDFs and nothing else. The tool is verified against those — it runs on all eight, reports columns on the two-column page, and shows a chunk spanning the page break on the section-across-pages page — but every question this step exists to answer needs a document nobody here wrote. Waiting on a file from the user. |

### Step 7.5 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-388 | discovery | **The worker cannot reach Postgres on Windows at all**, and this is not a network problem | `psycopg` refuses to run async on the `ProactorEventLoop` and raises before attempting any I/O. `app/worker/__main__.py` calls plain `asyncio.run`, which on Windows gets exactly that loop, so every database call the worker makes fails identically whether the database is reachable or not. Reproduced against an unresolvable host: on the default loop the error is the loop guard, on a selector loop it proceeds to real DNS resolution. `tests/integration/conftest.py` already forces `WindowsSelectorEventLoopPolicy`, so this was known once and fixed only where the tests needed it. A hard blocker for 7.6. |
| A-389 | discovery | The API survives only because the documented run command happens to use `--reload` | Uvicorn picks `ProactorEventLoop` on Windows unless it is running with a subprocess, which `--reload` and `--workers` both are. So `uvicorn app.main:app --reload` works and plain `uvicorn app.main:app` — which is what a deployment runs — would fail on the first query with the same loop error. |
| A-390 | correction | The environment verifier reports a Postgres failure it cannot avoid | Its check runs under `asyncio.run` with no policy set, so it fails on the loop guard before touching the network, and has done so on every run. Any conclusion drawn from that line about connectivity was drawn from a message that says nothing about connectivity. This is separate from the Wi-Fi symptom recorded earlier, which was a connection that opened and then returned no bytes. |
| A-391 | discovery | Installing the group made the test suite nine times slower, 26 seconds to 242 | `_build_embedder` and `_build_reranker` load model weights inside `build_container`, and sixteen tests build a container, each paying about thirteen seconds to put two models on the GPU. Nothing was wrong before because both slots were unimplemented stubs. The wiring question those tests ask is which adapter sits in which slot, which does not need weights. Left as it is pending a decision, since making the slots lazy changes when a worker discovers a broken model. |
| A-392 | discovery | Explicit normalisation in the embedder is redundant for this model | `bge-small-en-v1.5` ends its own pipeline with a `Normalize` module, so vectors come back unit length whether or not the adapter asks for it — turning the flag off changes nothing, which a mutation confirmed by not being caught. The flag stays: it is insurance against a model swap, which is a configuration change plus a reindex by design, and the test asserts the property rather than the flag. |
| A-393 | assumption | The instruction prefix this model documents does not help here, on a small probe | `bge` recommends prefixing queries with a retrieval instruction. Measured over three questions against three passages, the margin between the right passage and the best wrong one was slightly worse with it every time — 0.226 to 0.222, 0.347 to 0.337, 0.139 to 0.119. Nine comparisons is not evidence of much, but there is no obvious gain being left unclaimed, and the current code, which prefixes nothing, is not a known defect. Revisit against the gold evaluation set. |
| A-394 | choice | The embedder tests run on the CPU while the configuration says CUDA | What is under test is the adapter contract — one vector per text, unit length, stable, batched, and closer to a paraphrase than to another subject — and none of that is a property of the hardware. Running them on the card would make the suite fail on a machine that merely has no GPU, and whether the card works is what the environment verifier answers. Marked slow, since they load real weights. |
| A-395 | correction | The dimension accessor was renamed and the old name warns on every call | Sentence-Transformers 5.6 renamed `get_sentence_embedding_dimension`; the old name still answers and emits a `FutureWarning` each time. In a worker log that is noise which teaches people to skim. The adapter now prefers the new name and falls back. |

### Event loop fix entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-396 | choice | The loop is chosen with `asyncio.run(..., loop_factory=...)` rather than by setting a policy | A policy is global state that the process has to set before anything creates a loop, and it is silently ignored by anything that passes its own factory — which is exactly what uvicorn does. Passing the factory at the call site means the choice is visible where the process starts, and `None` already means "decide for me", so the call reads identically on every platform. `app/runtime.py` holds the decision and the reason. |
| A-397 | discovery | **The API cannot choose its own loop, so it checks instead** | Uvicorn resolves a loop factory from its own table and passes it to `asyncio.run`, which means any policy the application sets is ignored. Confirmed by asking a `uvicorn.Config` directly: with `reload=False` it answers `ProactorEventLoop`, with `reload=True` it answers `_WindowsSelectorEventLoop`, and setting a selector policy first changes neither. So startup asserts the loop it was handed is usable and refuses to serve otherwise. Verified by starting the server without `--reload`: it now fails at startup naming the loop, rather than accepting requests and failing each one. |
| A-398 | discovery | The whole unit suite had been running on a loop the application cannot use | Only the integration conftest forced the selector policy, so every other test ran on the proactor loop. Nothing showed it because unit tests mock the database — but it meant the suite was exercising an arrangement that cannot exist, and the new startup check failed against the suite while passing in every configuration anyone runs. The policy moved to the root conftest and applies to every test; the integration copy is gone. |
| A-399 | correction | Two of the new tests were reading the ambient default rather than naming a loop | Once the root conftest settled the default to a working one, the tests asserting that the proactor loop is refused started passing for the wrong reason and then failing. They now name both loops explicitly, which is what they meant in the first place. |
| A-400 | discovery | Postgres is reachable, and had been all along behind this fault | With the loop fixed, the environment verifier connects in about a second: PostgreSQL 17.6 with `vector` 0.8.2, `rum` 1.3, `pg_cron` 1.6.4 and `pg_trgm` 1.6 all present. Only Ollama, which is not installed, and R2, which is unconfigured, are still outstanding. This does not retract the Wi-Fi finding recorded earlier, whose symptom was different and specific; it does mean that failure and this one are separate, and that the verifier could not have told them apart. |
| A-401 | choice | The startup check refuses rather than warns | A warning would leave a server running that fails every request, which is the situation being fixed. Failing at startup costs one restart and names the cause, the alternative surfaces one query later as an error that describes a loop and says nothing about who chose it. |

### Integration suite, first run

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-402 | discovery | The integration suite passes, and had been unrunnable for two separate reasons | It needed `TEST_DATABASE_URL`, which was never set, and before that it needed the event loop fix — the same fault that made the environment verifier report a connection failure. Fourteen tests passed against PostgreSQL 17.6: the schema matches the models on a real database rather than SQLite, the migration chain is at head, and the streamed conversation writes commit. Durability on that path was argued in step 9.15 and is now demonstrated. Closes A-283. |
| A-403 | choice | The destructive migration round-trip stayed off | It runs `alembic downgrade base` against whatever `TEST_DATABASE_URL` points at, and here that is the only database this project has. It is guarded by its own flag, which was left unset. Reversibility of the migration chain therefore remains unproven, and needs a disposable instance rather than a decision. |
| A-404 | discovery | The database is empty, which confirms nothing has ever been ingested | Every table read zero rows after the suite cleaned up: knowledge bases, conversations, documents, chunks, messages, evidence records and jobs. So the run left nothing behind, and separately, no document has completed ingestion in the lifetime of this project. Everything known about the pipeline is still inference from fixtures. |

### Step 10.1 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-405 | choice | The caller no longer says how much evidence to send | `RetrieveEvidenceQuery.top_k` and `AnswerCommand.top_k` are gone. One number is wrong for one of any two questions: it dilutes a direct factual answer with four passages that drag it toward whatever they happened to say, and starves a comparison of the second subject so that it compares one thing against nothing. Neither reads as a retrieval failure afterwards, because the passages were relevant and the ranking was right. |
| A-406 | choice | The order the three limits win in is the substance of the step | Token budget over everything, because passages past the limit are truncated by the model from the end rather than sent. Class minimum over the score margin, because the margin says what the ranking believes while the minimum says what the question requires — a comparison whose second source scored poorly is still a comparison, and two passages let the model say the sources cover one side where one produces a confident half-answer. One passage over the class minimum, because a prompt with no evidence is a question answered from the model's memory. Each of the three is pinned by a test, and swapping the middle pair is caught. |
| A-407 | choice | Per-class counts are a domain table, not twenty-six environment variables | The tuning-values decision covers thresholds that need calibrating against evaluation data. These are ranges over question kinds — a rule about questions — and promoting them to configuration would make the rule unreadable while calibrating nothing, since there is no evaluation set to calibrate against yet. The table is injectable, so any range that later earns calibration can be lifted out alone. The reranker margin and the global bounds stay in configuration, where they already were. |
| A-408 | correction | The global ceiling was not actually a ceiling | Clamping a class range into the configured bounds resolved a conflict in favour of the class: a class wanting three passages under a ceiling of two returned a range of two to three, and the ceiling that is supposed to bound every ordinary query could be raised by the table it was meant to bound. Caught by the test written for it before anything downstream saw it. The ceiling now wins and the class collapses onto it. |
| A-409 | discovery | A mutation escaped because the fixtures were too tidy to tell two behaviours apart | Returning the selected passages without renumbering them passed every labelling test, because the fixtures arrived already numbered one to n in final order — so renumbering and doing nothing produced identical output. Real candidates reach the selector carrying labels from fusion and an order changed by reranking. The fixtures now arrive with reversed labels and ranks, and the mutation is caught. |
| A-410 | assumption | Only part of what evidence selection must weigh is weighed here | Reranker score, relative margin, token budget and count are in. Diversity and source coverage are the caps in step 10.2 and are not considered yet; modality requirements need the visual pipeline that Phase 6 has not built. The step is complete against what it can decide today, not against the whole of the requirement it starts. |

### Step 10.2 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-411 | choice | Pruning runs before the count is chosen, not after | Selecting five passages and then discovering three were the same one leaves two, having spent the token budget on the copies. Pruning first means the count is chosen over passages that are all still saying something. The pipeline now reads rerank, prune, select, and the prune stage is timed and logged like the rest. |
| A-412 | choice | Passages are compared by containment rather than by a symmetric score | A paragraph quoted whole inside a section is entirely redundant with it, and a measure dividing by the union rates that pair low precisely because the section is longer — so the redundancy that matters most is the one it would miss. Replacing containment with a union-based ratio fails the test for it. |
| A-413 | choice | The comparison is over runs of words, not sets of words | Two passages on one subject share most of their vocabulary while saying different things; only repetition shares the order as well. Five-word runs are long enough that ordinary shared phrasing does not match and short enough that a sentence reworded at one end still does. |
| A-414 | choice | The preference for distinct sources in a comparison is a tightened cap, not a reordering | The requirement says a comparison should prefer distinct sources. Implemented by halving what one document may supply rather than by promoting passages from unseen documents, because promoting would silently overrule the reranker — the one stage that looked at the query and the passage together. Halving guarantees a second document gets in wherever it has anything relevant, without touching the order. |
| A-415 | discovery | The rule protecting the best passage was unreachable, and a mutation proved it | Skipping the checks for the first candidate looked load-bearing and was not: with an empty kept list and every counter at zero, the first passage passes every rule anyway. Making the first candidate obey the rules changed no behaviour and no test failed. The guarantee now comes from the shape of the loop — the best passage is admitted outright and the loop starts after it — so no rule added in a later step can quietly acquire the power to drop it. The requirement holds structurally rather than by arithmetic that happens to work out. |
| A-416 | choice | A passage counts against the page it starts on, and only that one | Chunks can span a page break. Counting such a chunk against both pages would let a page reach its cap while contributing less of the answer than the cap implies. |
| A-417 | assumption | Two of the six kinds of repetition have never been exercised | Duplicate visual descriptions and repeated graph evidence are both listed in the requirement and both handled by the same text comparison as everything else, but nothing produces either yet: figures are stored without descriptions until Phase 6, and no graph evidence exists until Phase 12. They are covered by argument, not by a test with the real thing in it. |

### Step 10.3 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-418 | choice | The rules are in the domain, the loading is not | Whether a fragment can be read on its own is decided from its own text and type, so it is a rule and testable without a database. Fetching the section it belongs to is I/O. `ExpansionRules` answers the first question, the orchestrator does the second, and the split is what lets twenty-six rule tests run without a session. |
| A-419 | choice | Expansion runs before the count is chosen | It changes what each passage costs, and the budget is spent in the step after. Deciding to send five passages and then tripling three of them would blow the allowance the count was chosen against. |
| A-420 | choice | Structured content is judged by what it is, never by how it opens | The prose rules read the first word: a lowercase opening means the sentence began in the previous chunk, because a boundary never lands inside a sentence. That reasoning is about prose and false for notation — a formula routinely opens with a lowercase symbol and a table row with a bare number. Found by a test: a formula that defined its own symbols, and therefore needed nothing, was being expanded because it started with `y`. |
| A-421 | choice | Two fragments from one section produce one expansion, not two | The second would become a copy of the first — the exact repetition the pruning step exists to remove, arriving after it has finished looking. The first is expanded and the later one is left as the fragment it was. |
| A-422 | choice | A parent that cannot be loaded leaves the fragment as retrieval found it | Deleted, or belonging to a scope this caller cannot see. Incomplete evidence is worse than complete evidence and much better than none, so the failure is silent by design rather than by omission. |
| A-423 | choice | The rules decline wherever they would have to guess | The asymmetry is the reason. A missed expansion leaves the passage exactly as it was, which is the status quo and usually fine; a wrong one replaces a precise passage with a section several times the size and spends the budget doing it. So the reference rule fires only on the opening word, where a pronoun genuinely reaches backwards out of the chunk, and not on pronouns further in, which almost always point at something the passage already introduced. |
| A-424 | correction | A test passed for a reason unrelated to what it asserted | The check that a self-contained fragment is left alone supplied no parent for it, so ignoring the rules entirely still left the fragment in place — there was nothing to swap in. A mutation that dropped the rules was caught by only one test. The parent is now supplied, so surviving means the rules declined rather than that expansion had nothing to reach for. |
| A-425 | discovery | `ChunkRepository` had no way to load several chunks at once | It could fetch one by id or every chunk in a document, and parent expansion needs neither: it wants a named handful, once, for a whole evidence list. `get_many` added to the port and the SQL repository, scoped like everything else, returning only what the caller may see and silently omitting the rest. |

### Step 10.4 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-426 | choice | Sentences that carry a negation, a number or a condition are never traded for space | They are the sentences whose loss changes the claim rather than shortening it. Removing "the method does not converge for non-convex losses" leaves prose that reads as an unqualified endorsement, fluently, with a citation attached to prove it — and nothing downstream can tell, because every word that remains is genuinely from the document. Where the protected sentences alone exceed the allowance the passage stays over budget, because a passage that fits and misleads is worse than one that is longer than planned. |
| A-427 | choice | Units get no rule of their own | A unit sits in the same sentence as its number, so keeping sentences with digits keeps both, and recognising a unit as such would mean a vocabulary that is wrong for every field the textbook is not about. |
| A-428 | choice | The generative flag raises rather than switching anything on | Generative compression is specified as disabled by default. Implementing it and defaulting it off would leave a path nobody exercises that silently rewrites evidence when flipped; refusing at construction records the decision where it will be read. A summarised passage is not the text the citation points at, which is the whole reason it stays off. |
| A-429 | choice | Compression runs last, on the passages actually being sent | Shortening something that was never going to be sent is wasted work, and shortening before the count would let a marginal passage in on the strength of a cut that discards the half that made it relevant. |
| A-430 | choice | The sentence splitter moved to `app/domain/sentences.py`, shared with the chunker | Chunking splits oversized paragraphs on sentences and compression selects whole sentences; two copies of the rule would let compression cut inside something chunking treated as indivisible, and the disagreement would surface as a passage that reads as though a clause went missing, with neither side wrong on its own terms. |
| A-431 | discovery | Three tests passed for reasons unrelated to what they asserted | The table test used a query that happened to match the title, so relevance selected it and the rule keeping it unconditionally was never exercised. The indivisible-content test used text with no sentence boundaries, so cutting it by sentence would have changed nothing. The already-fits test used text whose re-join was identical to the original, so rebuilding it was invisible. All three now discriminate: a query that matches neither title nor headings under a budget with room for two lines, a caption written as real sentences, and a passage whose paragraph break would be flattened by a needless rebuild. |
| A-432 | assumption | A table split across chunks loses its headers on every piece but the first | Chunking divides an oversized table on its rows, and only the first piece carries the caption and column headings. Compression keeps the opening lines of whatever chunk it is given, so for a later piece those lines are data rather than headings. The requirement to retain headers therefore holds for tables that fit in one chunk and cannot be met here for tables that do not — the fix belongs in chunking, which is where the header is dropped. |
| A-433 | assumption | Graph evidence is specified and cannot be exercised | Compression must retain a relationship, its passage and its provenance. Nothing produces graph evidence until Phase 12, so the rule is unwritten rather than untested: the compressor treats such evidence as prose today, which would cut it by sentence. To be revisited with the graph retriever rather than guessed at now. |

### Step 10.5 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-434 | choice | `ModelRequest` grew from seven slots to the twelve FR-CTX-01 specifies | Not a new type alongside the old one: the seven-slot version is what every caller already built, so extending it in place is what let the change land in one step rather than two parallel structures drifting apart. `memory_context` split into `pinned_memory` and `relevant_memory`, matching the spec's two distinct memory slots; `mandatory_requirements`, `knowledge_base_state`, `rolling_summary`, `output_schema` and `critical_checklist` are new and default to absent, since nothing in this codebase produces any of them yet. |
| A-435 | choice | Evidence now carries the label the model must cite it by | `LabeledPassage` bundles a plain string label with the passage text, deliberately not the retrieval package's `EvidenceLabel` — a provider-neutral request should not import a type from the package that produced it. Before this, `ollama.py` joined passage text with no label at all, so the model had no way to say which passage it was citing and Phase 11 would have had nothing to validate. This is the change the whole step existed to make. |
| A-436 | choice | The context builder owns one thing: what gets shed when the assembled prompt does not fit | Ordering (FR-CTX-01) and budgeting (FR-CTX-02) are stated as one requirement pair but are different problems — order is about what the model reads first, budget is about what happens when everything does not fit. Shedding clears a slot to its empty value rather than trimming it, because a half-sent Knowledge Base summary or half a conversation history reads as complete and is not. |
| A-437 | choice | Five things are never shed by this module: identity, safety rules, the task, the evidence, the question | Losing any of them would not shrink the prompt so much as break what it is for. Evidence in particular already passed through four steps (10.1-10.4) that sized and compressed it to its own budget; the context builder has no authority to decide which passage goes; that decision belongs upstream. Verified by mutation: making safety_rules shed cleanly fails the test written for exactly that case. |
| A-438 | choice | Shedding is written as a tuple of typed functions, not a name-and-value lookup table | A `dict[str, object]` fed through `dataclasses.replace(**overrides)` is what a first pass produced, and mypy correctly refused it — replace is typed per-field, and a dynamic dict erases that. Eight explicit `lambda r: replace(r, field=empty)` entries keep every shedding step checked against the entity it targets, at the cost of the table being longer to read than a loop over a dict would have been. |
| A-439 | choice | Memory stays an acknowledged user/assistant exchange in the Ollama adapter, not folded into the system message | The system block gained several new slots (mandatory requirements, Knowledge Base state) and folding memory in as well was the simpler first draft, but it discarded a deliberate existing technique: a fact presented as something the model has read and confirmed is attended to differently than the same fact inside an instruction it was never asked to acknowledge. Memory, the rolling summary, and evidence each keep their own turn; only the purely structural slots share the system block. |
| A-440 | correction | Conversation order changed to match FR-CTX-01: recent turns now precede evidence | The prior adapter injected memory and evidence before history. The twelve-slot order puts recent raw turns at position 8 and evidence at position 9, so the rendered order changed to match — recent turns, then evidence, then the question, then the output schema, then the checklist. Pinned to the rendering by four tests that check relative position rather than presence. |
| A-441 | choice | `ModelSettings.prompt_token_budget` (8000) must exceed `EvidenceSettings.context_token_budget` (6000) | The same pattern as chunking's parent-exceeds-child rule: a prompt budget no larger than the evidence budget would let the passages alone fill the whole prompt, leaving no room for the instructions and the question that make the passages answerable. Enforced by a top-level cross-settings validator, following the precedent already set for the reranker/candidate-pool pair. |
| A-442 | assumption | Most of the new slots have no producer and are exercised only by direct construction | Mandatory requirements, Knowledge Base state, pinned memory, relevant memory, a rolling summary, an output schema and a critical checklist are all specified, all structurally present, and all empty in the one caller that exists today. `AnswerUseCase` populates only what phases 0-10 have built: the safety rules, the task, conversation history and labelled evidence. The rest becomes real as phases 8, 11 and 14 land, and the shedding order was chosen with all twelve in mind rather than guessed at when the seventh caller shows up. |
| A-443 | choice | Conflicts between instructions are declared by whoever produced them, never inferred here | Whether "keep it brief" contradicts "explain each step fully" is a judgement no deterministic rule can make, and a model call to decide it would put a guess in the path of every prompt. So an instruction may name the subject it governs — answer length, language, citation style — and two instructions naming one subject are treated as competing. An instruction naming no subject competes with nothing and always survives, which is the safe default: failing to notice a conflict sends both, inventing one drops something the student asked for. |
| A-444 | choice | A security rule claims its subject outright rather than winning a contest for it | Sending a security rule alongside the preference it outranks and trusting the priority labels to settle it leaves the model holding a contradiction, which is the thing resolution was supposed to have removed. So anything non-security naming a subject a security rule names is absent from the prompt, not merely outranked within it. Two security rules on one subject both survive, because a contest between them would have to drop one and a dropped security rule is the single outcome this is built to prevent. |
| A-445 | choice | Recency decides before correctness does, once level has already decided | The ordering that suggests itself is level, then whether the instruction was an explicit correction, then when it was said — and it is wrong. A correction made in the morning would still bind after the student asked for something different in the afternoon. So the key is level, then time, then correction, which still satisfies the rule a correction was introduced for: a correction must carry a timestamp, so against a standing preference carrying none it has already won on recency. Being a correction only breaks exact ties. Verified by mutation: reordering the two fails the test written for the afternoon case. |
| A-446 | choice | A security rule below CRITICAL cannot be constructed | Rather than checked wherever it might matter. A security rule that binds less than critically could be outranked by a preference, and every later guard against that would be guarding a state the type system can refuse outright. A correction with no timestamp is refused for the same reason: it supersedes by being more recent, so one with no time would silently behave as the oldest thing in the set. |
| A-447 | choice | Requirements are numbered after resolution, in final reading order, and the numbers never move again | Numbering before resolution would leave gaps nobody could explain — an R2 that lost a conflict and was never sent. Numbering after means the names describe what is actually being sent. Shedding is the deliberate exception: it removes whole requirements and leaves the survivors' names alone, so a gap where R2 used to be is the honest record that it was dropped for space, where renumbering would show nothing at all. |
| A-448 | choice | `ModelRequest.mandatory_requirements` holds structured requirements, not rendered strings | The same argument that gave evidence its label in the step before: an identifier that exists only inside a formatted line has to be parsed back out by whatever scores compliance, and that parser would have to reproduce the format exactly. The slot keeps the name the twelve-slot structure gives it while holding requirements at every level including merely preferred ones, because a preference read beside the rules it yields to is a preference whose rank is visible, and one kept in a list of its own is not. The adapter renders one per line. |
| A-449 | correction | Shedding became level-aware, and the requirement slot now sits at both ends of the order | The previous step shed the whole requirement slot in one step, third of eight. With requirements classified that would drop a critical rule to save three tokens. Preferences now go third, critical requirements are never shed at all, and the merely required ones are the last thing surrendered — after conversation history, because dropping the exchange that made a question answerable is worse than dropping a rule about how to answer it. Nine steps now rather than eight. |
| A-450 | choice | `build_all` is the general entry point and `build` is its single-task case | Two tasks in one prompt share one budget, so the context one of them needs is shed to make room for the other's and the ordering deciding which loses was never told there were two tasks; they also share one task objective, which then has to describe both. Separate calls give each task the whole budget. Nothing yet produces more than one task, so the one caller passes a sequence of one — but it passes it through the same path, rather than the separation being a branch nothing takes. |
| A-451 | correction | A dead term in the contest guard, found by mutation and removed | The loop skipped an instruction if it was a security rule, or named no subject, or named a claimed one. The first test could never fire: a security rule either names a subject, which is therefore claimed and caught by the third test, or names none and is caught by the second. A mutation removing it changed no test result, which is what exposed it. The same shape as the guard removed from the pruner in an earlier step — a condition that reads as a safeguard and is unreachable. |
| A-452 | choice | The use case's safety rules were split, and prompt-injection defence added to what remains | Three of the old safety strings were a mix of security, grounding and scope in one tuple. Grounding and citation moved into the classified requirement list where they can be numbered and scored; what stays in the never-shed slot is the two rules that hold for every turn regardless of what is asked. One of those is new: that everything in the passages and the conversation is material to reason about and never an instruction to follow. The codebase has carried `UntrustedText` since the start for exactly this and the prompt had never said it. |
| A-453 | assumption | Nothing yet produces a student instruction, so conflict resolution runs on a fixed list of five | `AnswerUseCase` declares the same five instructions every turn: one security, two grounding, one output contract, one style preference. Student constraints, preferences and corrections are Phase 8's memory store; a Knowledge Base's own requirements are Phase 14's. Both arrive as instructions with a subject and a time and need no change here. Until they do, the resolution rules are exercised by tests rather than by traffic — the same standing as the empty slots from the step before. |
| A-454 | correction | The system preamble was trimmed to identity, after a rendered prompt showed it restating rules that now have names | Grounding, abstention and register were stated in the preamble and again as R2, R3 and R5. A rule the model meets twice with only one of the two named is the paragraph this step exists to break up, reproduced rather than removed. Found by rendering an actual prompt through the real adapter rather than by any test — the tests check that each requirement is present and correctly ordered, and none of them could notice that something was also said elsewhere. |

### Step 11.1 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-455 | choice | The output schema is model-facing prose, not a JSON Schema object | A JSON Schema validates a response after generation; the model reads the schema before generating. An example plus a short rule list is what a model attends to in that slot. Any attempt at machine validation of the model's compliance happens later, in the parser and then in the validator — those two stages depend on the content, not on the schema format chosen here. |
| A-456 | choice | Extra keys in the model's response are tolerated at every nesting level | Models annotate: chain-of-thought keys, confidence scores, alternative phrasings. Refusing any key not in the three named ones would break on every model that annotates more than asked. The downstream parser reads exactly what it names and ignores the rest, so the tolerance is free. |
| A-457 | correction | The `GeneratedAnswer` invariant was extended to forbid non-empty claims alongside `insufficient_evidence` | The original check only enforced the non-empty-claims-when-not-insufficient direction. A response that says "I have no evidence" but also supplies cited claims is self-contradictory — the validator would have to decide which half to trust. The domain type refuses to hold that state, so the parser's job is to detect it and raise before the contradiction reaches anything downstream. |
| A-458 | choice | `_parse_claim` extracted from `parse_generated_answer` to keep branch count under the linter threshold | `PLR0912` fires at thirteen branches. Moving the per-claim parsing into its own function is the natural refactor here — each claim is an independent object, and its invariants are best verified where that object is constructed. |
| A-459 | choice | `_TASK_INSTRUCTIONS` updated to mention JSON format, not a new numbered requirement | The task instruction slot is never shed; a requirement slot can be. A format instruction in a slot that could go missing is a format instruction that could go missing. The output schema slot carries the full detail; the task instruction carries one sentence that survives even the most aggressive compression. |

### Step 11.2 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-460 | correction | `OUTPUT_SCHEMA` example fixed from `["S1"]` to `["[S1]"]` | The adapter renders each evidence passage as `[S1] text`, so the label that "appears beside the passage" is `[S1]` with brackets. The schema example showed `S1` without brackets, which is the format the model would follow — then every citation would fail the existence check because `LabeledPassage.label` is `"[S1]"`. Fixed so the example is consistent with what the model actually sees. |
| A-461 | choice | The citation existence check runs against the evidence that was sent, not the full knowledge base | A label from a passage that retrieval ranked highly but the context builder shed for token budget counts as unseen. The model could not have read it, so citing it is fabrication in the only sense that matters: the answer rests on a passage the model did not have. The validator carries a `Sequence[LabeledPassage]` rather than a repository reference so this boundary is enforced by the type. |
| A-462 | choice | Label matching is exact and case-sensitive, with no bracket normalisation | The schema instructs the model to cite the label as it appears. A system that normalises `S1` to `[S1]` is silently correcting a mistake the model made by not following the instruction; fixing the instruction is the right response, not introducing silent tolerance that makes future mistakes invisible. |
| A-463 | choice | `CitationCheckResult.fabricated_labels` is a `frozenset` rather than a list | The claim already holds citations as a tuple to preserve order; what matters for the existence check is membership, not sequence. A frozenset makes the set-ness of the result explicit and makes the caller's `has_fabricated_citations` branch branchless. |

### Step 11.3 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-464 | choice | ENTAILED beats CONTRADICTED in `aggregate_claim_status` | If one passage entails a claim and another contradicts it, the claim is still supported — the model had genuine evidence. The CONTRADICTED path applies only when no passage entails and at least one contradicts, which is the case where the model is asserting something while citing a source that says the opposite. Both pieces of information survive in the per-passage results; the aggregated status is the signal for the decision stage. |
| A-465 | choice | The entailment adapter builds one ModelRequest per passage, not a batch | A single request containing all cited passages could cause the model to conflate them — deciding that "the passages collectively support the claim" rather than checking each one. One request per passage keeps the judgment isolated: the model sees exactly the claim and the one source it is supposed to support. |
| A-466 | choice | `max_tokens=10` set on the entailment request | The answer is one of three words. A token budget of ten is generous enough to accommodate whitespace around the word while being tight enough to prevent the model from adding explanatory text that would break the one-word parser. |
| A-467 | choice | `parse_entailment_status` normalises case before lookup | The model may capitalise inconsistently, particularly when the response includes stray whitespace. Normalising to uppercase catches the variants without introducing a fuzzy match that could accept an unintended value — the lookup is still an exact match against three keys. |

### Step 11.4 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-468 | choice | `decide()` placed in `validation.py`, not a new module | The function aggregates the outputs of `check_citation_existence` and `aggregate_claim_status` — both already in `validation.py`. A separate module would need to import from it regardless, adding a file boundary without adding a layer boundary. |
| A-469 | choice | A claim with every citation fabricated is REJECTED, not REPAIRABLE | There is no real passage to point to — the claim is groundless. A repair attempt operates on the same evidence set; retrying cannot conjure citations that were never there. REPAIRABLE is for cases where some real evidence exists but the answer does not represent it accurately. |
| A-470 | choice | CONTRADICTED maps to REJECTED, not REPAIRABLE | A repair attempt generates from the same evidence. If the evidence says the claim is false, a new generation attempt has no mechanism to produce a claim the evidence supports instead. Returning REPAIRABLE would trigger a retry that is certain to fail, costing a round-trip for nothing. |
| A-471 | choice | `entailment_by_claim` is `Sequence[Sequence[EntailmentResult]]`, parallel to `citation_results` | The caller already has per-claim results from calling `check_claim` once per claim. A flat sequence would require `decide()` to group by claim internally — a linear scan over a collection the caller organised a moment ago — with no benefit to clarity or correctness. `strict=True` on the zip turns a length mismatch into an immediate error rather than a silent truncation. |

### Step 11.5 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-472 | choice | `build_repair_instructions` placed in `validation.py`, not a new module | The function consumes `CitationCheckResult`, `EntailmentResult`, and `aggregate_claim_status` — all already in `validation.py`. Splitting it out would add a file boundary without adding a layer boundary and would force a cross-import for what is, in effect, the fourth pass over the same data structures. |
| A-473 | choice | Each bullet names the exact claim text | A generic "some claims need fixing" forces the model to guess which ones. Naming the claim text ties the instruction to the specific output item that needs changing, which is as precise as a repair prompt can be given that the model may have paraphrased its own claim internally. |
| A-474 | choice | Fabricated labels are listed explicitly in the bullet | The model invented a reference it cannot have read. Showing it the exact label prevents it from re-inventing the same one on the next attempt. The sort is deterministic so tests can assert on the output without accommodating iteration order. |
| A-475 | choice | Empty string returned when there are no issues, rather than raising | The function is documented as "called only when REPAIRABLE" but the caller in 11.6 may check the result before acting on it. An empty return is less surprising than an exception in the rare case where the function is called defensively and the answer happens to be clean. |

### Step 11.6 entries

| ID | Kind | Summary | Detail |
|---|---|---|---|
| A-476 | choice | `generate_stream` called in `execute()`, not inside `_tracked()` | Tests that inspect `gateway.generate_stream.call_args` do so before consuming the stream. Moving the call inside `_tracked()` would require all those tests to consume the stream first, which would make the assertions indirect. Calling it in `execute()` keeps the call observable immediately while still leaving collection and validation inside the generator. |
| A-477 | choice | `GenerationRejectedError` raised inside the `try` block to set `failed=True` | The assistant message must be saved with FAILED status when an answer is rejected, for the same reason it is saved on any other error — the question was asked, the model ran, and the result is part of the turn record even if it cannot be shown. Raising inside `try` is the simplest way to trigger the existing FAILED path in `finally`. |
| A-478 | choice | Repair only attempted when decision is REPAIRABLE; REJECTED goes straight to error | A repair attempt regenerates from the same evidence. If the evidence already contradicts the claim or every citation was fabricated, nothing changes on retry. A REPAIRABLE result has real evidence that could be cited correctly; a REJECTED one does not. |
| A-479 | choice | `_validate` and `_check_entailment` extracted to module-level functions | Both functions are pure pipeline steps that do not need `self`, and module-level placement makes them directly testable and usable from the repair path without threading `self` through closures. The closure in `_tracked()` captures the `entailment` adapter and `labeled` passages instead. |
| A-480 | choice | Default test gateway response uses `insufficient_evidence=true` | Avoids requiring every test that does not care about validation to supply real evidence and a working entailment mock. An `insufficient_evidence` answer passes validation without any claims to check. Tests that exercise specific validation paths provide their own JSON response. |

## Step 11.7 — Security tests: generation pipeline

| ID | Kind | What | Why |
|---|---|---|---|
| A-481 | deviation | `test_evidence_record.py` updated alongside new test file | After Step 11.6 added `entailment` as a required `AnswerUseCase` parameter, the existing security test file was calling the constructor without it and using a gateway that returned non-JSON tokens. Both would cause `TypeError` / `GenerationRejectedError` in the now-mandatory validation path, breaking all 9 existing evidence-record tests. Fixed in the same step rather than leaving a broken test suite. |
| A-482 | choice | Gateway default in `test_evidence_record.py` changed to `insufficient_evidence=true` JSON | The existing tests care about the evidence record path, not the validation outcome. Using a valid `insufficient_evidence` response lets the pipeline succeed without needing evidence or entailment setup, which is not what those tests are about. The two tests that exercise failure paths use their own gateway inline. |
| A-483 | choice | Entailment mock in `test_evidence_record.py` returns empty tuple | The `insufficient_evidence` gateway response triggers `decide()` to return `INSUFFICIENT_EVIDENCE` immediately, before any claim loop runs. Entailment is never called. A trivial `AsyncMock(return_value=())` is sufficient and avoids constructing `EntailmentResult` objects for a path that is never reached. |
| A-484 | choice | Four tests in `test_generation_security.py` rather than three | The anti-injection invariant has two distinct sub-cases: injection present in evidence, and no evidence at all. The PLAN description says "prompt injection inside a PDF" but the injection safety rule must also hold when there is no evidence — both are tested separately. The fabrication invariant splits into fully-fabricated (one call, direct REJECTED) and partially-fabricated / cross-scope (repair loop runs, two calls). |
| A-485 | finding | Fully fabricated citations skip the repair loop; `generate_stream` is called once | When every citation in a claim is fabricated, `real_citations` is empty and `decide()` sets `rejected = True` without checking entailment. Because REJECTED is already set, the overall decision is REJECTED and no repair is attempted. The repair path is only entered when `decide()` returns REPAIRABLE, which requires at least one real citation alongside the fabricated ones. The test assertion was corrected from `call_count == 2` to `call_count == 1`. |
| A-486 | choice | Cross-scope citation test uses `ClaimStatus.ENTAILED` for the real passage | The test's goal is to show that a partially-fabricated answer cannot escape rejection. Making the real citation ENTAILED isolates the fabrication as the only failure cause — a NOT_SUPPORTED real citation would also produce REPAIRABLE and then REJECTED, but through a different code path, obscuring which invariant the test is actually enforcing. |

## Step 11.8 — Citation provenance and resolution

| ID | Kind | What | Why |
|---|---|---|---|
| A-487 | finding | `Citation` and `Evidence.to_citation` already existed from Phase 1 but were never called by application code | Only three tests referenced `to_citation`; nothing in `app/` did. The §40 value object was built early and then orphaned, so the phase looked further from citation support than it was and closer to it than the wiring justified. This step gave it a producer rather than writing a second citation type beside it. |
| A-488 | choice | `resolve_citations` placed in `app/domain/retrieval/entities.py`, not `models/validation.py` | The package boundary runs one way: `retrieval/` already imports `models/` in `expander.py` and `rewriter.py`, while nothing in `models/` imports `retrieval/` — `LabeledPassage` documents that as deliberate. The resolver needs `GeneratedAnswer` and `Evidence` together, so it belongs on the side already permitted to reach across. |
| A-489 | choice | `chunk_type` added as a required field, `element_type` as optional | Every chunk has a `chunk_type`, so a default would be a lie available to silently mislabel a table as prose. `element_type` is genuinely absent for chunks the layout parser never typed, so `None` is the honest state rather than a gap. One existing test constructing `Citation` directly was updated. |
| A-490 | deviation | §40's "object" (table or figure id) not added to `Citation` | `Chunk` carries no `table_id` or `figure_id`; those arrive with Phase 6, which has not started. A field nothing can populate would be scaffolding indistinguishable from a bug at read time. Recorded as the remaining part of FR-CIT-02 instead. |
| A-491 | choice | `evidence_hash` populated from `Chunk.content_hash` | The field already existed and was never filled. Reprocessing rewrites chunks, so a citation whose hash no longer matches its chunk is pointing at text that has since changed — detectable only if the hash is captured at citation time, which costs nothing here. |
| A-492 | choice | One citation record per (claim, label) pair, not per claim | A claim resting on two passages is traceable to two places, and one record would drop one. The same passage cited by two claims likewise yields two records, because the question asked later is which passage supports *this* claim, not which passages the answer touched. |
| A-493 | choice | `citation_order` runs across the whole answer, and numbers only records that were written | Restarting per claim makes the value useless for sorting the set back into the order the answer argues in. Numbering surviving records rather than attempted labels keeps the sequence contiguous — a gap would imply a record exists that nobody wrote. |
| A-494 | choice | Unresolvable labels are skipped rather than raising | A validated answer has none: the citation existence check rejects or repairs those first. But this function writes the permanent record, and a row built on a label nobody can resolve is worse than a shorter set. Malformed labels take the same path via `EvidenceLabel.try_parse`. |
| A-495 | deviation | Fixed `get_answer_use_case`, broken by Step 11.6 | Adding the required `entailment` parameter to `AnswerUseCase` never reached the FastAPI dependency, so `POST /conversations/{id}/stream` would have raised `TypeError` on every request. mypy caught it; no test did, because every route test overrides the dependency with a mock. Fixed in this step rather than left on the only path a real caller takes. |
| A-496 | choice | `OllamaClaimEntailment` constructed in the dependency rather than added as a container slot | It owns no resource of its own — it is the already-wired gateway plus a prompt. The same dependency already builds `ContextBuilder` inline for the same reason. |
| A-497 | choice | Added two tests that call `get_answer_use_case` directly | Every existing route test overrides it, which is correct for those tests and is exactly why the constructor and its only caller drifted apart without a failure. A test that calls the real dependency is what closes that gap. |

## Step 11.9 — Persisting citations

| ID | Kind | What | Why |
|---|---|---|---|
| A-498 | choice | Location, type and hash are copied onto the citation row rather than joined through `chunk_id` | Reprocessing rewrites chunks. A citation resolved against the current chunk at display time would silently begin describing a passage the answer never saw, which is the failure this table exists to make impossible. The row states what was true when the claim was made. |
| A-499 | choice | Composite primary key `(message_id, citation_order)` | Prevents duplicates and fixes the order the answer argues in using the key itself, so the set reads back in sequence without a sort column that could disagree with the key. |
| A-500 | choice | `save_citations` is a separate port method from `save_retrieval_chunks` | They answer different questions and legitimately differ: a passage can reach the prompt and support nothing. One records what the model could have known, the other what it actually used, and collapsing them would make the gap between the two invisible — which is the gap worth seeing. |
| A-501 | choice | `merge` rather than `add`, matching `save_retrieval_chunks` | The composite key would reject a second write for the same message. Merge lets an answer be regenerated without first deleting its previous citations. |
| A-502 | choice | `chunk_id` and `document_id` use `ON DELETE CASCADE` | Consistent with the sibling bridge table and with `Citation.chunk_id` being non-nullable in the domain. See A-503 for the cost this carries. |
| A-503 | finding | Reindexing will erase the citation history of past answers | Chunks are deleted and recreated when a document is reprocessed, and CASCADE takes the citation rows with them. The denormalised columns survive the *content* changing but not the *row* being replaced. Worth revisiting when reindexing is actually built — a nullable `chunk_id` with SET NULL would keep the record at the cost of a citation that resolves to no chunk. Not changed now, because inventing the half-built alternative before there is a reindex path to test it against would be speculative. |
| A-504 | choice | Label stored unbracketed as `S1` | The brackets are how a label is printed, not what it is. `EvidenceLabel.bracketed` renders them where they are needed. |
| A-505 | choice | Citations resolved before the first token is yielded, saved in the `finally` block | Resolution needs the evidence set that issued the labels, which is in hand at that moment; afterwards the labels are numbers in a string nobody can resolve. Saving happens with the rest of the turn record so both writes land in the same unit of work. |
| A-506 | choice | A rejected or abstaining answer writes an empty citation set, not no call | Nothing was cited, and an empty record says so. Skipping the call entirely would leave the two states — "cited nothing" and "was never asked" — indistinguishable. |
| A-507 | deviation | `_ev` in the use-case tests rebuilt as a real `Evidence` instead of a `MagicMock` | Citation resolution looks the cited label up in a dict keyed by `EvidenceLabel`. A mock attribute never equals a real label, so every citation silently resolved to nothing and the first assertion on saved citations failed. The label has to behave like a label for the test to mean anything. |
| A-508 | deviation | Fixed pre-existing `PLR0917` on `_make_use_case` by making it keyword-only | Six positional parameters since step 11.6; ruff had been failing on the file since. Every call site already passed keywords, so the fix was free. |
| A-509 | assumption | Migration `0009` has not been applied to a live database | Alembic reports `0009 (head)` and the chain from `0008` is clean, but applying it needs a Postgres connection the current network blocks. The RLS policy in particular is unverified against a real database — the SQLite tests cover the table shape and the repository writes, not the policy. |

## Step 11.10 — Model metadata from the streaming path

| ID | Kind | What | Why |
|---|---|---|---|
| A-510 | finding | The Ollama adapter already read the counts and threw them away | `generate_stream` decoded the final NDJSON line to check `done`, and that same line carries `prompt_eval_count`, `eval_count` and `done_reason`. The metadata was never missing from the provider — only from the contract, which yielded bare strings and had nowhere to put it. |
| A-511 | choice | `generate_stream` returns a `TokenStream` rather than gaining a second port method | A parallel `generate_stream_with_usage` would duplicate the adapter and leave two paths to keep in step. Making the returned object both iterable and able to report its own cost keeps one implementation, and `async for` over it reads exactly as before. |
| A-512 | choice | `usage` is `None` until the stream is exhausted | The counts do not exist until the provider has finished producing, so any earlier value would be invented. The fake stream in the tests sets it on drain for the same reason — one that set it up front could not distinguish a correct implementation from one reading it too early. |
| A-513 | choice | Absent usage is stored as null, never zero | A provider that reported nothing and a call that genuinely cost nothing are different facts, and a stored zero collapses them. Every metadata column stays null unless the provider actually said. |
| A-514 | choice | `_collect_stream` takes `AsyncIterable[str]` and reads usage via `getattr` | The honest minimum the function needs is iterability. `getattr` keeps it working with the bare async generators used as test doubles across four other test files, which would otherwise all have needed rewriting to satisfy a protocol they do not care about. |
| A-515 | choice | A repaired answer records the repair call's usage, not the first attempt's | What is stored describes the generation that produced the answer actually returned. The discarded attempt is not part of it. This does mean the rejected attempt's cost goes unrecorded — acceptable while there is no per-invocation table, and the natural place to fix it is `model_invocations` in Phase 8. |
| A-516 | choice | A rejected answer still records what it cost | The stream is drained before validation refuses the result, so the tokens were spent. A turn that produced nothing usable is exactly the one worth being able to see the cost of. |
| A-517 | deviation | `_mock_gateway` rebuilt around a `_FakeTokenStream` class | It returned a bare async generator, which cannot carry a `usage` attribute. The fake also had to be constructed per call rather than once, because the repair path calls `generate_stream` twice and a single generator is exhausted after the first. |
| A-518 | assumption | `prompt_version` is not part of this step | It versions the prompt, not the model, and needs a column, a migration and a policy for what changes the version. Kept separate rather than folded into the metadata work it happens to sit beside in the checklist. |

## Step 11.11 — Prompt version

| ID | Kind | What | Why |
|---|---|---|---|
| A-519 | choice | `prompt_version` is derived by hashing the template, not hand-maintained | A constant someone must remember to bump is one that eventually lies, and a version that lies is worse than no version: it makes two different prompts look like one, which is exactly the comparison the field exists to support. Deriving it means editing any instruction changes the version without anyone remembering to. |
| A-520 | choice | Covers preamble, safety rules, task, numbered requirements and output schema | These are what this module decides. Slot ordering belongs to the context builder and per-turn content is not template, so neither is included. The consequence worth naming: reordering the twelve slots would not change the version. |
| A-521 | choice | Parts joined with `\x1f` before hashing | A separator that cannot occur in the text means two different splits cannot produce the same string. Joining on a printable character would let a rule containing that character imitate a boundary. |
| A-522 | choice | Format is `answer-` plus twelve hex characters, no human-maintained major | A readable prefix says which prompt it is; the digest carries everything else. A hand-set major number would reintroduce the drift the derivation exists to remove, for readability that the prefix already provides. |
| A-523 | choice | Computed at import rather than per turn | The template is module-level and cannot change at runtime. Recomputing a SHA-256 for every answer would spend work to reach the same result. |
| A-524 | choice | No shared fingerprint helper extracted | There is one prompt today. A second caller — Phase 15's study-content prompts — is the point at which the shape of a reusable helper is knowable; guessing it now would be building the abstraction before the case that constrains it. |
| A-525 | choice | `prompt_version` is deliberately not tied to `model_id` | The existing metadata invariant requires the three token fields together. This one stands alone: a turn can be refused before the provider reports anything, and the prompt that was sent is still worth naming. Recorded on FAILED assistant messages for that reason. |
| A-526 | choice | Column is nullable and old rows are not backfilled | User messages are not produced by a prompt, and answers written before the column existed have no recoverable version. Backfilling would mean asserting which template produced them, which nobody recorded. |
| A-527 | deviation | Fixed two API tests broken by the new invariant | `_msg_row` builds a `MagicMock` and nulls every optional column explicitly, but `prompt_version` did not exist when it was written — so the attribute auto-created as a truthy mock and read as a prompt_version on a user message. Same failure mode as A-507: an unset attribute on a mock is not `None`. |
| A-528 | assumption | Migration `0010` has not been applied to a live database | Alembic reports `0010 (head)` and the chain from `0009` is clean, but applying it needs a Postgres connection the current network blocks. `0009` is in the same state. |

## Step 11.12 — Cancellation on disconnect

| ID | Kind | What | Why |
|---|---|---|---|
| A-529 | finding | A disconnected turn was recorded as COMPLETED, and could store the placeholder text as the answer | `asyncio.CancelledError` and `GeneratorExit` both derive from `BaseException`, so `except Exception` never saw them while `finally` still ran — the turn was saved with `failed=False`. Reproduced both ways before fixing: closing the iterator mid-stream stored a COMPLETED answer nobody received, and cancelling during generation stored `"(generation failed)"` as a COMPLETED answer. The second is the damaging one: that string becomes assistant content and is loaded back as conversation history on the next turn. |
| A-530 | choice | Added `MessageStatus.CANCELLED` rather than reusing FAILED | Nothing went wrong when a student closes the tab, and the two call for different responses — abandonment is a signal about the interface, failure is a signal about the answer. The column is `String(20)` and the value is new, so no migration is required. |
| A-531 | choice | Abandonment outranks failure in `_outcome` | A student who has already left cannot be told about a failure, so what the record should say is that they left. Ordering it the other way would relabel most disconnects as failures, since cancellation frequently interrupts work that would also have raised. |
| A-532 | choice | Placeholder text chosen per status instead of one shared default | `"(generation failed)"` under a CANCELLED status would be a second small lie. `answer_text` now starts as `None` and the placeholder is selected at write time, so each outcome says what actually happened. Content still cannot be blank — the entity refuses it. |
| A-533 | choice | `GeneratorExit` handled alongside `CancelledError` | They arrive by different routes — task cancellation on disconnect versus a consumer closing the iterator — but mean the same thing to the record. Handling only the first would leave `aclose()` recording a completed answer. |
| A-534 | deviation | The SSE endpoint now closes the answer stream in a `finally` | `async for` over an inner generator does not close it; that was left to garbage collection. Since the turn is only recorded in the generator's cleanup, the record had no guaranteed arrival time. |
| A-535 | choice | `execute` returns `AsyncGenerator[str, None]` rather than `AsyncIterator[str]` | Closing is part of the contract now, and `AsyncIterator` has no `aclose`. The wider type was understating what the method actually returns and what callers must do with it. |
| A-536 | deviation | Extracted `_record_turn` from the `finally` block | Adding the cancellation branch pushed `execute` past ruff's statement limit. The persistence block was the natural extraction: it is one coherent operation — write the answer, its evidence, its citations — and it reads better named than inline. |
| A-537 | finding | FAILED and CANCELLED messages are still loaded as conversation history | `list_messages` does not filter on status, so a placeholder like `"(cancelled before an answer was produced)"` is replayed to the model as a previous assistant turn. `MessageStatus.was_delivered` was added as the predicate this filtering will need, but the filtering itself is not part of this step — it changes what the rewriter and retriever see, which deserves its own step. |
| A-538 | assumption | Persistence during cancellation was verified, not shielded | Both reproductions confirmed the writes complete: a single delivered cancellation still allows awaits inside `finally`. A second cancellation arriving mid-cleanup — server shutdown during a disconnect — would interrupt it. `asyncio.shield` would close that gap; it was left out rather than added against a case that could not be demonstrated. |

## Step 11.13 — Undelivered turns out of prompt history

| ID | Kind | What | Why |
|---|---|---|---|
| A-539 | choice | Added `list_history` rather than a flag on `list_messages` | The two consumers want different things and both are right: the API shows the student their transcript, failures included, while the prompt must carry only what was actually said. A boolean argument that changes what a query means would leave the reason at the call site; two names put it in the method. |
| A-540 | choice | The status filter runs in SQL, not in the caller | It has to be applied before the limit. Taking the last ten messages and then discarding the failed ones leaves fewer than ten turns of context, and fewest exactly when the conversation has been going badly — which is when context matters most. |
| A-541 | choice | Questions are kept whatever became of their answers | The student did ask, and a follow-up is only intelligible next to what preceded it. Filtering on delivery alone would have dropped every user message too, since a question never leaves `RECEIVED` — the predicate is role-aware for that reason. |
| A-542 | deviation | Updated the repository mocks in three test files | They stubbed `list_messages`; the use case now calls `list_history`. An `AsyncMock` auto-creates the missing method and a `MagicMock` iterates as empty, so every affected test would have silently run with no history at all rather than failing. Third instance this phase of a mock quietly supplying the wrong thing (A-507, A-527). |
| A-543 | finding | `MessageStatus.was_delivered` is defined but unused | Added in step 11.12 as the predicate this filtering would need. The filter is expressed in SQL against the column, so the domain property has no caller. Left in place as the name for the concept, but it is currently ornamental — worth removing if nothing in the presentation layer picks it up. |

## Step 11.14 — Faithfulness: the prose against its own claims

| ID | Kind | What | Why |
|---|---|---|---|
| A-544 | choice | Faithfulness compares the answer to its claims, not to the passages | The claims have already been checked against the passages one by one, so an answer covered by its claims is covered by the evidence through them. Checking whole prose against a single passage would instead call ordinary connective and explanatory writing unsupported, and reject good answers for being written like answers. |
| A-545 | choice | `AnswerFidelity` is its own two-value enum rather than reusing `ClaimStatus` | The question is different: `ClaimStatus` asks whether evidence supports a claim, this asks whether the claims cover the prose. `CONTRADICTED` has no meaning here — an answer cannot contradict its own claims without also overstating them — and a three-value enum with one unreachable member invites handling that can never run. |
| A-546 | choice | `OVERSTATED` maps to REPAIRABLE, never REJECTED | The evidence is sound and nothing was invented; the prose simply says more than was established. Rewriting it to match the claims is exactly what a second attempt can do, which is the definition of repairable. |
| A-547 | choice | A separate `AnswerFaithfulnessPort` rather than a method on `ClaimEntailmentPort` | Entailment is claim-against-passage and returns one result per pair; this is answer-against-claims and returns one verdict per answer. Sharing a port would mean one implementation serving two prompts, two shapes and two failure modes. |
| A-548 | choice | The faithfulness request carries no evidence, history or memory | A passage in context invites the model to find support the claims never made — which is the exact failure the check exists to catch. It sees the answer and the claims and nothing else. |
| A-549 | choice | The check is skipped for REJECTED and INSUFFICIENT_EVIDENCE | It costs a model call and neither outcome can move: a rejected answer cannot be rescued by it, and an abstaining one made no claims for its prose to overstate. Kept for REPAIRABLE, where it cannot change the decision but does add a bullet to the repair instructions. |
| A-550 | deviation | Assumed only REJECTED needed skipping; the abstention test caught the second case | `decide()` short-circuits on `insufficient_evidence` before it ever looks at fidelity, so the verdict was computed and then discarded — a wasted model call on every abstaining answer. Found by the test rather than by reading. |
| A-551 | choice | `_validate` returns a `_Validation` record instead of a tuple | The tuple was already four wide and the fidelity would have made it five, with the repair call unpacking positionally. Naming the fields is what keeps the repair step's use of them readable. |
| A-552 | choice | `AnswerUseCase.__init__` made keyword-only | Six collaborators tripped ruff's positional-argument limit. Every call site already passed keywords, so the change cost nothing and removes a class of argument-order mistake. |
| A-553 | finding | The integration tests had been broken since step 11.6 and nobody knew | `test_answer_persistence.py` was still constructing `AnswerUseCase` without `context_builder` or `entailment`, and feeding the gateway plain prose tokens that the parser would now reject. They skip without `TEST_DATABASE_URL`, so three steps of drift accumulated silently. Repaired here: the collaborators are supplied and the fixture emits a valid abstaining response. Still unverified against a real database, for the same network reason as `0009` and `0010`. |

## Step 11.15 — Verification against the live database

| ID | Kind | What | Why |
|---|---|---|---|
| A-554 | correction | Migrations `0009` and `0010` are applied; the database is at `0010` | Both had been written and left pending across six steps (A-509, A-528). Applied cleanly from `0008` in one run. Both are additive — a new table and a nullable column — so nothing existing was rewritten. |
| A-555 | correction | The `message_citations` RLS policy is verified against PostgreSQL | SQLite cannot express row-level security, so the policy had only ever been read, never executed. `pg_class.relrowsecurity` is true and `message_citations_user_isolation` exists with `polcmd='*'`. The composite primary key `(message_id, citation_order)` and every column's nullability match what the ORM declares. |
| A-556 | correction | `MessageStatus.CANCELLED` fits the column it goes in | `status` is `varchar(20)` and the new value is nine characters. Step 11.12 asserted no migration was needed; this confirms it rather than assuming it. |
| A-557 | finding | `test_migrations_at_head` had a hand-written `_EXPECTED_HEAD = "0008"` | It sat at `0008` through two migrations and only surfaced when these tests were first run again. Replaced with the head read from the alembic script directory: what the test means is "the database matches the migrations", so that is what it now asks, and it cannot go stale. Third hand-maintained constant this phase to drift (see A-519 on prompt version). |
| A-558 | finding | The citation write path had never touched a real database | Every integration turn abstained, so `save_citations` returned early with nothing to write. The new table's foreign keys and composite key were unexercised. Added `_run_cited_turn` and four tests that write and read back a real citation row. |
| A-559 | correction | `_evidence` fabricated a `document_id`, which `message_citations` will not accept | Harmless while only `conversation_retrieval_chunks` was written — it keys on `chunk_id` alone — but `message_citations` carries a foreign key on `document_id`, and a random one is refused by the database. The fixture now carries the seeded document through. Precisely the class of bug SQLite tests cannot find. |
| A-560 | assumption | The destructive migration round-trip is still unrun | `test_migration_round_trip` drops and rebuilds the whole schema and stays gated behind `ALLOW_DESTRUCTIVE_MIGRATION_TEST=1`. It should not run against this database, which holds real data. The full `0001..0010` chain is therefore verified forward but not as a round trip. |

## Step 11.16 — Register refresh

| ID | Kind | What | Why |
|---|---|---|---|
| A-561 | correction | Four Phase 11 checklist items re-marked; the tally is now 7 done, 3 partial, 1 not started | `[S1]` provenance moved from not-started to partial, and semantic validators, SSE cancellation and persistence all moved from partial to done. Each mark had been left behind by the step that satisfied it — the checklist is only useful if it is re-read after the work, not only before it. |
| A-562 | choice | `[S1]` provenance is `[~]` rather than `[x]` | Document, page, type, bounding box and content hash all land, which is what the Phase 19 viewer needs. Only §40's *object* is missing, and it cannot be built here: a chunk carries no table or figure id until Phase 6 creates them. Marking it done would hide a real gap behind a satisfied-looking box. |
| A-563 | correction | The Phase 11 prose paragraph was describing the opposite of the current state | Written at step 11.8, it said labels were bare ordinals, no `message_citations` table existed, and §40 was unmet — all three fixed within the next four steps. Rewritten around what the turn now leaves behind and why the two records are deliberately different. |
| A-564 | correction | `test_container.py` no longer fails and has been removed from the known-failures list | It was listed as asserting `model_gateway` is an unimplemented slot. The whole file passes; only the `test_stage_timer` timing flake remains, which the last full run did not trip. |
| A-565 | finding | Registers went stale roughly every second step this phase | Three separate refreshes were needed across 11.7–11.16, and each found marks contradicting the code. The pattern is that a step updates `EXECUTION_LOG.md` — the standing constraint says it must — but nothing obliges it to revisit the checkbox that motivated it. Worth folding the re-mark into each step rather than batching it into a periodic sweep. |

## Step 11.17 — The three remaining §38 rules, and a check for figures

| ID | Kind | What | Why |
|---|---|---|---|
| A-566 | choice | All three new rules bind `CRITICAL` under `GROUNDING_AND_SOURCE_USE` | Each is about how the model may treat its sources, which is what that category means, and none is a preference worth shedding under budget pressure. The context builder drops PREFERRED first and REQUIRED last; a rule that may be dropped at all is not a rule. |
| A-567 | correction | `PROMPT_VERSION` moved from `answer-8b882012f9be` to `answer-4b7f5e6131bd` on its own | Nobody edited it and nothing had to remember to. This is the first real exercise of the derivation added in 11.11, and it behaved as designed — three instructions changed the template and the version followed. |
| A-568 | choice | FR-GEN-06 got a deterministic validator as well as a rule | The checklist asks for rules "enforced structurally", and a rule with no check is enforced only by hope. FR-GEN-03 and FR-GEN-07 are behavioural and a rule is the enforcement available; a figure, uniquely among the three, can be checked by looking. |
| A-569 | finding | Entailment cannot catch an altered figure, which is why this check is separate | A model reading "rose from 100 to 150" and writing "rose by 50%" is saying something the passage supports, so the claim is ENTAILED and every semantic check passes. The figure is still one the student cannot find when they follow the citation. Rounding `3.14159` to `3.14` fails the same way. |
| A-570 | choice | Figures compared by value, not by spelling | `1,000` in a passage carries `1000` in a claim, and `0.50` carries `0.5` — formatting is not a change of quantity, and flagging it would bury real findings under noise. What the comparison will not carry is a different number, which is the whole point. |
| A-571 | choice | Numbers written as words are not matched against digits | Writing "three stages" where the passage wrote "3 stages" has not changed the quantity. Matching prose against digits would flag well-written answers and teach the repair loop to make them worse. |
| A-572 | deviation | Citation labels are stripped before any figure is read out of a claim | Every label is full of digits and none of them is a quantity, so `[S1]` would otherwise read as the figure 1 and match against any passage containing one. Found while writing the test for it rather than after. |
| A-573 | choice | An unsupported figure is REPAIRABLE, never REJECTED | The right number is sitting in the evidence and the model can be told to use it, quoted back as it wrote it so it can find what to change. Nothing was fabricated at the level of sources — only at the level of a digit. |
| A-574 | choice | The check runs beside the citation check, before any model call | It is deterministic. A figure the passages do not contain costs nothing to find, and finding it early means an answer already destined for repair does not pay for an entailment pass first. |

## Step 11.18 — Partial abstention (UC-09 A1)

| ID | Kind | What | Why |
|---|---|---|---|
| A-575 | choice | A partially supported answer returns what stands instead of being refused | UC-09 A1 asks for it, and refusal is the worse outcome for a student who asked something the material half covers: they learn nothing, including the fact that half of it was answerable. Previously one `NOT_SUPPORTED` claim among three sent the whole answer to `REJECTED` after the repair. |
| A-576 | choice | Salvage is attempted only from REPAIRABLE, never from REJECTED | A rejection means a citation was invented or the evidence refutes the claim. Answering around either would quietly admit the thing the validation gate exists to stop, so the gate is applied to the decision before any salvage is considered. |
| A-577 | choice | The prose is rebuilt from the surviving claims rather than kept from the model | The model wrote its prose to carry every claim it made. Dropping claims underneath it would leave prose asserting what no longer stands — precisely what the faithfulness check exists to catch, and it would be the salvage that introduced it. |
| A-578 | finding | Rebuilding the prose turns out to be the right remedy for overstatement too | `test_a_persistently_overstated_answer_is_refused` began failing because the answer now returns. That is correct: overstatement lives in the prose, the claims behind it are sound, and rebuilding from those claims removes the overreach rather than the whole answer. The test was renamed and rewritten to assert the trimming. |
| A-579 | correction | Salvage first shipped a claim carrying an invented figure | The claim was ENTAILED, so it survived — and it still contained a number no passage held, defeating the check added one step earlier. `build_partial_answer` now takes the numeric results and drops any claim with an unsupported figure. Caught by the 11.17 pipeline test, not by review. |
| A-580 | correction | Salvage first shipped a claim built on a fabricated citation | A claim citing one real and one invented label is entailed by the real one, so it survived with the invented label stripped. Stripping is not enough: the model invented a source, and keeping the claim ships something built on that invention. Any fabricated citation now disqualifies its claim outright, which restores the cross-scope security gate that had started passing an answer through. |
| A-581 | choice | Salvage applies the same bar the validators do, not a looser one | It is the one path that returns an answer nobody re-validated. Both corrections above were the same mistake — treating entailment as sufficient when the pipeline requires entailment *and* real citations *and* faithful figures. A claim let through here is one shown to a student with no check behind it. |
| A-582 | choice | A contradiction stops salvage entirely rather than dropping the one claim | A contradiction is not a gap. It means the model misread the material, and answering around it would present the rest as though the misreading had not happened. |
| A-583 | choice | What was dropped is named, framed as unsupported rather than restated as fact | A student not told what is missing cannot tell a partial answer from a complete one. But the dropped text came from a model and failed validation, so repeating it plainly would hand over the very assertion the evidence declined to back. |
| A-584 | choice | The result sets `insufficient_evidence=False` | Something was answered. An abstention and a partial answer are different outcomes, and collapsing them would lose the distinction the frontend needs in order to render them differently. |
| A-585 | deviation | `build_partial_answer` does not take the original answer | It was a parameter until ruff pointed out nothing read it. Nothing in the original survives — the claims are re-derived and the prose is rebuilt — so the signature now says so. |

## Step 8.1 — Gateway façade + task router

| ID | Kind | What | Why |
|---|---|---|---|
| A-586 | choice | `ModelGatewayFacade` created in `infrastructure/models/gateway.py`, not alongside the Ollama adapter | The façade is infrastructure (it holds concrete adapters), but its logic is routing-only and applies equally to all providers. Keeping it separate from any one adapter makes it clear that it has no provider-specific code. |
| A-587 | choice | `TextGenerationCapability` gains `generate_stream` | The façade must delegate all three operations the gateway port exposes — non-streaming, streaming, and image. Without `generate_stream` on the capability Protocol, the façade could not call it on a provider without losing type safety. |
| A-588 | choice | `MultimodalCapability` extends `TextGenerationCapability` rather than repeating `profile` and `generate` | A multimodal provider can do everything a text provider can, so the relationship is subtyping, not composition. Removing the duplicated members makes the Protocol hierarchy say what it means. |
| A-589 | choice | `generate_with_image` routing checks both `supports_images` and `supports_task(VISUAL_QUESTION)` | A profile may have `supports_images=True` for some non-VISUAL_QUESTION purpose (e.g., an encoder that does not do QA). Requiring both guards against selecting such a provider for multimodal QA. Found by a test that was initially written the wrong way. |
| A-590 | correction | Initial test `test_provider_with_supports_images_false_not_selected_for_image` used an impossible profile | `ModelProfile.__post_init__` rejects `VISUAL_QUESTION` in tasks when `supports_images=False`. Replaced with `test_provider_without_visual_question_task_not_selected_for_image`, which tests the reachable scenario: `supports_images=True` but task not declared. |
| A-591 | choice | `OllamaModelGateway` unchanged — still satisfies both `TextGenerationCapability` and the previous `ModelGatewayPort` | The Ollama adapter already had all three required methods. Wrapping it inside the façade required no changes to the adapter itself; only the wiring in `wire.py` changed. |
| A-592 | choice | `wire.py` wraps the single Ollama adapter in a `ModelGatewayFacade([ollama])` | The container slot still holds a `ModelGatewayPort` — nothing in the application layer changed. The façade is transparent to all callers. |

## Step 8.2 — Privacy pre-flight (§52)

| ID | Kind | What | Why |
|---|---|---|---|
| A-593 | choice | `privacy_sensitive` is a derived property on `ModelRequest`, not a constructor parameter | A parameter could be set incorrectly (e.g., forgetting `True` when evidence is present). A derived property is always consistent with the actual slots in the request — if evidence is there, the request is private. |
| A-594 | choice | The property treats evidence, memory, history, and rolling summary as private; the query alone is not | The query is the student's question, but for tasks like faithfulness checking the "query" slot carries the model's own output being verified, which is not student-identifiable. Marking the query alone as private would make every single request private and make the check meaningless for all-local setups. |
| A-595 | choice | `_enforce_privacy` is a private method on the façade called in all three route-and-call methods | The check belongs in the same place as routing — the façade. Putting it in a helper keeps each of `generate`, `generate_stream`, and `generate_with_image` to two lines rather than repeating the same conditional. |
| A-596 | choice | The error is always fatal — no fallback on boundary violation | §52 is explicit: rerouting to a permitted provider defeats the purpose of the boundary. A student who configured a LOCAL-only data policy must get an error, not a silently rerouted request. |
| A-597 | finding | `DataBoundaryViolationError` already carried the right fields (`provider`, `boundary`) from Phase 8's earlier work | Nothing new needed on the error type. The test asserts both fields to pin the contract. |

## Step 8.3 — Fallback chain (§53)

| ID | Kind | What | Why |
|---|---|---|---|
| A-598 | choice | The provider list IS the fallback chain — the first capable provider is tried, then the second, etc. | A separate per-task fallback config would duplicate the provider order already expressed in the list. The list order is preference order, which is all a fallback chain needs to be. |
| A-599 | choice | Non-retryable `ProviderError` propagates immediately, bypassing remaining candidates | A malformed request or context-length violation won't be fixed by switching providers — retrying wastes time and may produce a confusing error from a different provider. `retryable=False` is the provider's signal that the request itself is the problem. |
| A-600 | choice | Privacy violation during fallback is fatal — the error propagates, the remaining provider list is not tried | Falling through to a third provider after a privacy violation on the second would silently ignore the boundary. The privacy check is an invariant of the request, not a property of one specific provider. |
| A-601 | choice | Streaming does not participate in the fallback chain | A `TokenStream` delivers tokens incrementally. By the time an error surfaces during iteration, some tokens may already have been sent to the caller. There is no safe rollback point at which to switch to a different provider, so the `ProviderError` goes straight to the caller. |
| A-602 | choice | `last_error` re-raised when all candidates fail | All-candidates-fail means a non-null `last_error` exists (candidates is non-empty by the guard above the loop). The `type: ignore[misc]` suppresses mypy's inability to see the loop's at-least-once invariant. |
| A-603 | choice | Fallback logged at WARNING with task and failed provider | A fallback is not an error — the request still succeeds — but it signals a degraded state. WARNING makes it visible in production without forcing an alert, and carrying the task and provider lets an operator correlate with provider health. |

## Step 8.4 — model_invocations table (§48, FR-MDL-26)

| ID | Kind | What | Why |
|---|---|---|---|
| A-604 | choice | `model_invocations` carries no `user_id` and no RLS | This is an observability table, not user data. No attribute of an invocation row reliably identifies a user (the trace_id is a request correlator, not an identity). Service-role-only access is the correct policy; adding a user_id column would require threading it through the gateway, which breaks the model gateway's provider-neutral contract. |
| A-605 | choice | `latency_ms` is nullable — streaming calls are excluded from invocation recording | A streaming response delivers tokens over time; there is no single end-to-end latency to record. The gateway's `generate_stream` method returns a `TokenStream` before any tokens arrive, so it cannot know final token counts or latency. Only `generate` and `generate_with_image` write invocation rows. |
| A-606 | choice | `cache_hit` column added at false by default even though caching is not yet implemented | FR-MDL-26 mandates it. Adding it now, at table creation, avoids a later ALTER TABLE on a table that may already have millions of rows. |
| A-607 | choice | `_record_invocation` catches all exceptions and logs a warning rather than propagating | A write failure on an observability row must not interrupt inference. The structlog event fires first (inside `write_model_invocation`, before `session.add`), so at least the log record exists even if the DB write fails. |
| A-608 | choice | `write_model_invocation` is now async and receives an explicit `AsyncSession` | The caller (the gateway's `_record_invocation`) opens the session in its own short-lived transaction, independent of any request-scoped session. Making the session explicit keeps the function's side effects auditable and avoids thread-local or global state. |
| A-609 | choice | `session_factory` passed to `ModelGatewayFacade` is `None` when no database URL is configured | `_build_model_gateway` in `wire.py` receives `None` when `db_url` is falsy. The gateway checks for `None` before opening a session, so dev runs without a database work silently. The alternative (passing the `_Unimplemented` sentinel) would produce noisy `NotImplementedError` warnings on every inference call in dev. |
| A-610 | choice | `used_fallback` is derived from the provider's position in the candidates list (index > 0 means fallback was used) | The gateway already knows which provider it is calling in the loop. Tracking position requires no extra state, and the semantics are exact: the first capable provider is the preferred one; any subsequent provider is a fallback. |

## Step 8.5 — Warm-up on startup (§55, FR-PRF-02)

| ID | Kind | What | Why |
|---|---|---|---|
| A-611 | choice | `warm_up_models` is a standalone async function, not a method on the gateway or the port | The port is a Protocol and adding `warm_up()` to it would require all future adapters to implement it, including stubs and mocks. The lifespan is the only caller; a standalone function avoids touching the Protocol surface. |
| A-612 | choice | Warm-up uses `ModelTask.ANSWER_GENERATION` with `max_tokens=1`, `temperature=0.0` | The single most important task to have loaded. `max_tokens=1` makes the response as fast as possible, and `temperature=0.0` makes it deterministic. The point is loading weights, not checking generation quality. |
| A-613 | choice | A failed warm-up logs a warning and returns — it never blocks the server from starting | If the model server is slow or temporarily down, refusing to serve any traffic at all is worse than the first request being slow. A warning in the logs is the right signal for an operator to investigate, not a hard startup failure. |
| A-614 | assumption | `localhost:11434` connection refused is near-instant — warm-up failure does not slow tests | The OS returns ECONNREFUSED before any timeout applies, so the unit tests that run the lifespan do not stall. Verified: the lifespan tests complete as quickly as they did before this step (latency is dominated by the pre-existing HuggingFace retries in the token counter, not by this change). |
| A-615 | choice | Warm-up fires unconditionally if `warm_models_on_startup` is True, regardless of whether the gateway has a session factory | The session factory controls invocation persistence, not warm-up. The two features are independent: warm-up does not write invocation rows (it uses no session). |

## Step 8.6 — OpenAI-compatible adapter skeleton (D-17)

| ID | Kind | What | Why |
|---|---|---|---|
| A-616 | choice | `_build_messages` extracted to a new `providers/prompt.py` module shared by Ollama and the OpenAI-compat adapter | Both APIs accept the same role-based chat messages array. One implementation in a shared module is the single source of truth; duplicating it would require keeping two copies in sync across every future twelve-slot change. |
| A-617 | choice | `build_chat_messages` replaces `_build_messages` without a compatibility alias | The function was module-private (`_`-prefixed), so nothing outside the Ollama adapter was supposed to import it. Only one test file imported it directly; that import was updated to the new canonical location in `prompt.py`. |
| A-618 | choice | `OpenAICompatibleGateway` defaults to `DataBoundary.THIRD_PARTY` | A bearer-token API is by definition an external service. The boundary is overridable at construction time, so a vLLM server with `api_key=""` can be wired as LOCAL; the default is the conservative/safe choice for any API key configuration. |
| A-619 | choice | `stream_options: {"include_usage": true}` added to every streaming request | Without it, token counts are unavailable on the stream. Servers that do not support this option (older versions) simply ignore the key and return no usage object; `GenerationUsage` then records zero for both counts rather than raising — the same graceful degradation as Ollama's `prompt_eval_count` defaulting to 0. |
| A-620 | choice | `GeminiGateway` and `AnthropicGateway` declared as real stubs with a live profile but `NotImplementedError` on all inference calls | D-17 mandates their presence in the adapter inventory. A profile that names real tasks lets the façade include them in future candidate lists once credentials are configured. Raising on every call makes "not yet implemented" impossible to miss. |
| A-621 | choice | `OpenAICompatibleGateway` added to `_build_model_gateway` only when `openai_compatible_base_url` is non-empty | An empty URL is the default value for the setting; leaving it empty disables the adapter entirely. This lets production run Ollama-only without any configuration change and lets the OpenAI fallback be added by setting one environment variable. |
| A-622 | choice | The OpenAI-compat adapter is appended after Ollama (second in the provider list) | Provider order is preference order. Ollama (LOCAL) is always tried first; the OpenAI-compatible endpoint (THIRD_PARTY by default) serves as a fallback when Ollama is unavailable. Putting it first would route every public-content request to the external endpoint even when the local model is healthy. |
| A-623 | choice | `generate_stream` fixture tests use a regular (non-async) `fake_stream_cm` | `httpx.AsyncClient.stream()` is a regular method that returns an async context manager, not a coroutine. The first attempt used `async def`, which returned a coroutine that the `async with` then failed on. Regular function + mock `__aenter__`/`__aexit__` is the correct shape for faking `client.stream`. |

## Step 8.7 — Prompt normalisation profiles

| ID | Type | Decision | Rationale |
|---|---|---|---|
| A-628 | choice | `PromptProfile` is a frozen stdlib dataclass, not a Protocol or Pydantic model | It is purely a data object carrying rendering options — no behaviour, no validation beyond Python's type system. Frozen dataclass is the lightest-weight option and is consistent with D-30 (domain entities are stdlib dataclasses). |
| A-629 | choice | `use_acknowledged_exchange` is the only field in the initial profile | FR-MDL-24 lists five adjustable axes (evidence placement, final instruction repetition, native JSON mode, system-message usage, token limits). The first four are rendering choices; only `use_acknowledged_exchange` is concretely useful now because token limits are already carried on `ModelProfile`. The other rendering axes can be added as named fields later without changing any call sites. |
| A-630 | choice | `build_chat_messages` default parameter is `DEFAULT_PROMPT_PROFILE = PromptProfile()` rather than `None` | A sentinel `None` would require a null-check at every call site or a guard at the top of the function. Using the constant as the default makes the function's behaviour self-documenting and keeps the common path (no profile argument) identical to the acknowledged-exchange path. |
| A-631 | choice | Adapters store `self._prompt_profile` and pass it explicitly; they do not call `build_chat_messages(request)` with the default | Relying on the default would hide the adapter's rendering choice from a reader of the adapter code. Explicit passthrough makes it clear that every adapter has a profile, even when that profile is the default. |
| A-632 | choice | `prompt_profile: PromptProfile | None = None` on adapter constructors, defaulting to `DEFAULT_PROMPT_PROFILE` internally | Callers that do not care about the profile omit the argument. The internal default is the constant, not the dataclass constructor, so there is exactly one `PromptProfile()` call in the module rather than one per adapter instantiation. `None` sentinel on the constructor is more readable at the call site than repeating `PromptProfile()` everywhere. |
| A-633 | observation | One test failure during development: Ollama wiring test used `AsyncMock(return_value=mock_resp)()` as the `side_effect` return, which produced a coroutine instead of `mock_resp`. Fixed by returning `mock_resp` directly from the `side_effect` function. |

## Step 8.8 — Data boundary security gate

| ID | Type | Decision | Rationale |
|---|---|---|---|
| A-624 | choice | Security gate tests placed in `tests/security/test_data_boundary_gate.py`, not appended to `test_gateway_facade.py` | The existing privacy pre-flight tests in `TestPrivacyPreFlight` verify routing behaviour; the gate file states the invariant as a release contract. Separating them lets `pytest -m "security and gate"` produce a focused report without filtering inside the unit test file. |
| A-625 | choice | Six gate tests, not the full `TestPrivacyPreFlight` suite | The gate expresses exactly the invariants that must hold for release: evidence→THIRD_PARTY blocked, memory→THIRD_PARTY blocked, streaming path blocked, multimodal path blocked, fallback cannot route around the gate, and non-private requests are not over-blocked. Detailed edge-case coverage (rolling summary, conversation history, individual `privacy_sensitive` field checks) belongs in the unit test file, not the gate. |
| A-626 | choice | `_stub_provider` in the gate file uses `MagicMock` rather than the `_StubTextProvider` class from `test_gateway_facade.py` | Each security test file is standalone — importing helpers from a unit test file would couple the gate to internal test infrastructure and make the gate file harder to read in isolation. The MagicMock stub is five lines and covers every method the façade calls. |
| A-627 | observation | Security test count increased from 115 to 121; gate count not separately tracked in `pyproject.toml` markers | The `gate` marker is registered but the count in PLAN.md status tracks all tests passing, not gate-marked ones specifically. Both counts are correct: `pytest -m "security"` passes 121, `pytest -m gate` is a subset. |

## Phase 9 item 4 — Model metadata investigation

| ID | Type | Decision | Rationale |
|---|---|---|---|
| A-634 | observation | The Phase 9 open item "model metadata always null because `generate_stream` yields bare strings" was stale — the implementation was already complete | Code audit confirmed: `OllamaTokenStream` and `OpenAICompatTokenStream` both implement `.usage` (a `GenerationUsage` set once stream is drained); `_collect_stream` in `answer.py` reads it via `getattr(stream, "usage", None)`; `_record_turn` passes all four fields to `Message`; `_msg_to_model` maps them to `MessageModel`. Unit tests in `test_ollama_gateway.py` (lines 682–756) verify `usage.prompt_tokens`, `usage.completion_tokens`, `usage.model_id`, and `usage.finish_reason` on the streaming path. The open item's description was written before the adapters gained `.usage`. |
| A-635 | choice | Marked the PLAN.md open item `[x]` with a corrective note rather than deleting the entry | The entry serves as a record that the omission was noticed and investigated; the corrective note prevents future readers from re-investigating the same phantom bug. |

## Phase 9 item 2 — Both forms stored (§24)

| ID | Type | Decision | Rationale |
|---|---|---|---|
| A-636 | choice | Added `RetrievalResult` dataclass to `retrieve_evidence.py` rather than returning a named tuple or changing the port interface | A frozen dataclass is consistent with the project's domain entity style, carries named fields without any footgun about position, and keeps both `evidence` and `was_rewritten` close to where they are produced — inside the orchestrator — without threading extra state through the port contract. |
| A-637 | choice | The early-exit path (`if not candidates: return []`) also returns a `RetrievalResult` with empty evidence | Returning a plain list from one branch and a dataclass from the other would cause the type checker to reject the return annotation and would require callers to branch on the type. Consistency matters more than brevity in an early return. |
| A-638 | choice | `was_rewritten=False` default in `_mock_retrieve` keeps all existing transaction-count tests unchanged | The three-transaction path only fires when the rewriter changes the query. Tests that verify "two transactions" or "one transaction" use the default `was_rewritten=False` and remain correct without modification. |
| A-639 | choice | The rewrite update opens a fresh unit of work rather than being batched with the initial user-message save | The two writes cannot share a transaction: the user message is committed before retrieval begins (the design intent — "a question that was asked stays asked however the rest of the turn goes"), and the rewrite result is only known after retrieval completes. Opening a new block after retrieval is the correct split. |
| A-640 | observation | `test_retrieve_evidence.py` used `result[i]` and `len(result)` throughout — changed assignment lines to `(await ...).evidence` so downstream indexing needed no edits | Replace-all on the assignment pattern was clean: 13 occurrences, all identical, all in the same file. |
